# 2026-09-21 로컬 기록 파일 결정을 Phase 4 선행 조건으로

`plan.md` 의 "다음에 할 일" 에서 로컬 기록 파일 형식 결정을 빼고 `roadmap.md` Phase 4 의
착수 전 항목으로 옮겼다. 당장 할 일이 아니라 Phase 4 의 선행 조건이기 때문이다.

## 변경

| 파일 | 내용 |
|------|------|
| `roadmap.md` (kor/eng) | Phase 4 작업에 착수 전 항목 추가. 경로와 줄 형식을 정하기 전에는 M-6을 판정할 수 없다 |
| `plan.md` | "다음에 할 일" 1번 삭제. 남은 것은 구현 Phase 1 하나다 |
| `plan.md` | 문서 부채 건수를 3에서 2로 정정 |

## 결정

**빼기 전에 그 결정이 어디에 걸리는지 먼저 정했다.**

`plan.md` 에서만 지우면 "Phase 4 착수 전에 정한다" 는 조건이 `architecture.md` 9장과
`spec.md` M-6 에만 남는다. 둘 다 Phase 4 를 시작할 때 펼쳐 보는 문서가 아니다. 그래서
`roadmap.md` Phase 4 의 착수 전 항목으로 옮겼다. `tools/nat-probe` 재측정 바로 다음이다.

재발 방지 규칙 4다. 수정이 만드는 새 계약을 함께 정의한다.

## 같이 고친 것

`plan.md` 가 문서 부채를 **3건**으로 적고 있었다. 실제 표에는 2건이다. 앞 커밋에서
`docs/eng` 지연 항목을 빼면서 요약 숫자를 안 고쳤다. **같은 파일 안에서 요약과 표가
어긋난 것**이다.

## 검증

- `python tools/docgate/docgate.py`: `VERDICT: pass`
- `plan.md` 안의 건수와 각 표의 행 수가 맞는지 다시 셌다

## 크로스 모델 리뷰

Codex 1라운드. 렌즈는 "빼면서 잃은 것이 있는가, 한국어와 영어가 같은 말을 하는가,
파일 안의 숫자가 자기 표와 맞는가" 였다. `LGTM - no blockers`.
