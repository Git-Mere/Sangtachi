# `architecture.md` 에서 "현재 상태와의 차이" 절을 뺐다

## 변경

- `docs/{kor,eng}/architecture.md` 10장 레포 구조의 `현재 상태와의 차이` 절을 지웠다

## 결정

**경과 서술이라 뺐다.** 그 절은 소스 트리가 아직 루트의 `src/main.cpp` 뿐이라고 적고 있었다.
`client/`, `control-server/`, `telemetry-server/` 가 이미 레포에 있으므로 현재 사양이 아니다.

**그 절이 지고 있던 의무는 `roadmap.md` 에 이미 있다.** Phase 1 작업 절의 "레포를 목표 구조로
재배치" 가 같은 것을 말한다. 지우기 전에 확인했다.

## 검증

- `docgate.py` 가 `VERDICT: pass`. 이 절을 가리키던 링크는 없었다. 헤딩 텍스트와 앵커 두 형태를
  모두 훑어 0건임을 확인했다
- 한국어와 영어에서 같은 절을 지웠다. 양쪽 10줄

## 크로스 모델 리뷰

Codex 에 삭제 렌즈로 돌렸다. 지우는 diff 는 없어져서 생긴 구멍이 보이지 않기 때문이다.

| 렌즈 | 결과 |
|------|------|
| 삭제. 그 절을 가리키는 다른 파일, 사라진 의무, 기록이 주장하는 `roadmap.md` 근거, 남은 문장의 일관성 | `LGTM - no blockers` |

지적이 없어 반영하거나 기각한 것이 없다.
