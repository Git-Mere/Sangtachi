# Plan

현재 작업 단위 체크리스트. Phase 단위의 큰 계획은 [`roadmap.md`](roadmap.md)에 있다.

출처는 설계 감사 5장 후속 계획표다. 각 항목의 blocker/warn 번호는
같은 기록 3장과 4장에서 찾는다.

> English version: [`../eng/plan.md`](../eng/plan.md)

## 후속 작업

| # | 작업 | 해소 | 상태 |
|---|------|------|------|
| 1 | `protocol.md` 확정본 작성 | blocker 1,2,3,6~10 + warn 13건 | 완료 2026-09-14 |
| 2 | 동시성 모델 확정 | blocker 4,5 + warn 2건 | 완료 2026-09-15 |
| 3 | Windows 사전조건 절 신설 | blocker 11~17 + warn 6건 | 완료 2026-09-21 |
| 4 | spec 논리 오류 수정 | blocker 18,19 + warn 3건 | 완료 2026-09-21 |
| 5 | 직접 연결 불가 대비책 | blocker 20 | 완료 2026-09-20 |
| 6 | 과잉 검증 기준 되돌리기 | warn 2건 (기준 4개) | todo |

**5번은 2026-09-20 실측으로 완료됐다.** 가장 위험한 항목이라 먼저 처리했다. blocker 20의
최악 시나리오는 관측되지 않았고 대비책을 도입하지 않기로 했다(5.7~5.9,
[ADR 0001](decisions/0001-직접-연결-대비책-미도입.md)). 조건이 바뀌면 다시 성립하는
잔여 위험이 남아 재측정 게이트를 세 지점에 뒀다.

**3번은 2026-09-21에 완료됐다.** 결과는 [`windows-prereq.md`](windows-prereq.md)다. 13개 항목이
각각 한 절을 갖고, 8개 절은 실제 Windows 11에서 명령을 돌린 출력으로 썼다(실측 4, 부분 실측 4).

**4번은 2026-09-21에 완료됐다.** 마일스톤 구분을 없애 주차 불균형 warn 을 원인째 지웠고, M-6 이 Phase 5 에 잘못 걸려 있던 것을 blocker 19 와 같은 결함으로 보고 함께 고쳤다.

**남은 것은 6번뿐이다.** 문서 작업이고 상대가 필요 없다.

---

## 3. Windows 사전조건 절

**완료 2026-09-21.** 결과는 [`windows-prereq.md`](windows-prereq.md)다.
검증 기준을 어떻게 충족했는지는 이 절 끝에 적었다.

**문제.** 문서 어디에도 실행 전제가 없다. 시연 PC에서 처음 돌릴 때 막힌다.

| 다룰 것 | 근거 |
|---------|------|
| 관리자 권한. 어댑터 생성과 라우트 설정에 필요 | blocker 11 |
| 방화벽. 새 어댑터가 Public 프로필로 분류되어 ICMP와 TCP 25565 인바운드 차단. 클라이언트 UDP도 첫 실행 프롬프트에 의존 | blocker 12 |
| 비정상 종료 후 잔존 어댑터/주소/라우트 정리와 재실행 시 중복 생성 방지 | blocker 13 |
| Wintun DLL/드라이버 패키징, 아키텍처(x64), 서명 | blocker 14 |
| `server.properties`의 `server-ip`가 다른 인터페이스 주소면 `10.100.0.1:25565` 접속 거부. 비워 두거나 `10.100.0.1`로 명시 | blocker 15 |
| EC2 보안 그룹 인바운드, 바인드 주소(`127.0.0.1` 금지) | blocker 16 |
| `10.100.0.0/24`가 실제 LAN, Hyper-V, 다른 VPN과 충돌할 때의 탐지와 대체 대역 | blocker 17 |
| on-link `/24` 라우트가 이미 생성되어 중복 생성 오류 | warn |
| 주소가 tentative 상태일 때 시험을 시작하면 간헐 실패 | warn |
| EC2 재시작 시 공인 IP 변경. Elastic IP 또는 DNS | warn |
| Minecraft LAN 탐색은 멀티캐스트라 유니캐스트 터널로 안 된다. 직접 IP 입력으로 우회 | warn |
| Java 업데이트로 경로 기반 방화벽 예외 무효화 | warn |
| SmartScreen/Defender가 미서명 클라이언트를 격리 | warn |

**사용성 한계도 함께 다룬다.** `spec.md`의 주장은 "수동 포트 포워딩 없이"이고 그 주장은
유지된다. 다만 대신 관리자 권한, 드라이버 설치, 방화벽 규칙이 필요하다. 설정 부담이 사라진
것이 아니라 종류가 바뀌었고, 포트 포워딩보다 쉬운지는 별도 논증이 필요하다. 보고서에서
정직하게 서술한다.

**검증.** 지금 시점에 실제 PC로 끝까지 가볼 수는 없다. Phase 1은 CMake 구성만 되어 있고
Winsock2 래퍼부터가 미착수다. 그래서 검증은 문서 자체에 대해 한다.

- 위 13개 항목이 각각 하나의 절을 갖는다
- 각 절에 확인 명령과 **기대 출력**이 있다 (`ipconfig`, `route print`, `netsh advfirewall`,
  `Get-NetAdapter`, `Get-NetFirewallProfile`). 명령만 있고 무엇을 보면 통과인지 없으면 미완
- 자동 처리와 수동 처리를 구분해 표시한다. 클라이언트가 하는 것과 사람이 미리 해둬야 하는 것

초기화된 PC에서의 실제 종단 검증은 Phase 8 시연 시점 기준이며 이 항목의 완료 조건이 아니다.

**검증 결과 (2026-09-21).**

| 기준 | 결과 |
|------|------|
| 13개 항목이 각각 한 절 | [`windows-prereq.md`](windows-prereq.md) 1~13절 |
| 확인 방법과 통과 조건 | 전 절에 있다. 12개 절은 명령이고 11절(LAN 탐색)은 수동 절차다. **실측 4절, 부분 실측 4절**이며 5개 절은 `미검증` 표시와 사유를 달았다 |
| 자동/수동 구분 | 14절 표 |

**넣기 전에 명령을 돌려 검토했고 두 가지가 나왔다** (재발 방지 규칙 6).

- `Get-NetFirewallProfile` 의 `DefaultInboundAction` 은 정상 기기에서 `NotConfigured` 다.
  `-eq 'Block'` 으로 검사를 쓰면 **정상 기기가 떨어진다.** 위 기준 목록이 이 cmdlet 을 예로
  들었으나 문서는 반례로만 쓰고 판정은 `netsh advfirewall` 로 한다
- Java 경로 점검을 `Format-Table -AutoSize` 로 찍으면 경로가 길어 **판정 열이 잘린다.**
  형식 문자열 출력으로 바꿨다

---

## 4. spec 논리 오류 수정

**완료 2026-09-21.** blocker 18, 19 와 성립성 warn 3건을 해소했다. 이 표가 그 완료의 출처다.

| 다룬 것 | 근거 | 결과 |
|---------|------|------|
| C-5 우선순위가 최소 산출물을 지키지 못한다 | blocker 18 | [`spec.md`](spec.md) 에 "우선순위 제거의 대가" 절을 넣어 등급마다 버릴 수 있는지와 버리면 사라지는 기준을 표로 적었다. `DATA` 최소 왕복과 로컬 기록을 P0 으로 옮겨 **최소 성공 M-1 ~ M-6 이 P0 만으로 성립**한다 |
| 추적표가 M-5를 Phase 4에 할당했으나 `DATA`는 Phase 5에나 생긴다 | blocker 19 | `DATA` 최소 왕복을 Phase 4 로 당겼다. **M-6 에도 같은 결함이 있었다.** 제어 평면 기록이 Phase 5 작업에 없었다. 로컬 기록 파일로 바꿔 Phase 4 에 뒀다 |
| M1 과 M2 의 작업량 불균형 | warn | **마일스톤 구분 자체를 없앴다.** 주차 배분은 진행 속도가 정하므로 문서에 고정하지 않는다. [`roadmap.md`](roadmap.md) 의 마일스톤 개요 절을 삭제하고 `spec.md` C-4 를 고쳤다 |
| Phase 4 keepalive 검증이 요구하는 외부 UDP 관측점이 설계에 없다 | warn | Phase 3 의 AWS EC2 제어 서버를 관측점으로 명시했다. **클라이언트는 probe 에 응답하지 않고 도착만 기록한다.** 응답하면 그 송신이 NAT 매핑을 갱신해 유휴 구간이 깨진다 |
| M-2 검증의 HTTPS IP 조회가 UDP 송신 경로와 다를 수 있다 | warn | 같은 UDP 소켓으로 서로 다른 두 STUN 서버에 질의하는 판정으로 바꿨다. HTTPS 조회는 참고 기록으로 내렸다 |

**같이 고친 것.** `protocol.md` 15장이 손실 집계를 Phase 4 와 Phase 5 양쪽에 적고 있었고,
머리글이 "Phase 4 착수 전에" 였다. Phase 4 가 그 체크리스트를 구현하는 단계다. 둘 다 고쳤다.
살아 있는 문서의 낡은 `protocol.md` 장 번호 참조 15건도 함께 맞췄다.

**남긴 것.** 로컬 기록 파일의 경로와 줄 형식은 정하지 않았다. 계약 4개(내용, 시점, 추가 전용,
독립성)만 [`architecture.md`](architecture.md) 9장에 적었고 **Phase 4 착수 전에 그 문서에서
정한다.** 코드에서 임의로 정하지 않는다.

---

## 5. 직접 연결 불가 대비책

**blocker 20. 가장 위험하다.** 가용한 두 가정망이 둘 다 목적지 의존 매핑이면 최소 성공이
불가능하다. 릴레이는 Phase 9 이후 스트레치라 구제가 안 된다. 프로젝트 전체가 통제 불가능한
외부 조건에 인질 잡혀 있다.

**먼저 할 것.** Phase 2 이전에라도 실측한다. 이 결정은 실측 없이 내릴 수 없다. 두 가지가
필요하고, **첫 번째만으로는 부족하다.**

1. **매핑 거동.** 두 개의 공개 STUN 서버에 Binding Request를 보내 관측 엔드포인트를 비교한다.
   **두 요청이 같은 bind된 UDP 소켓과 같은 로컬 엔드포인트를 써야 한다.** 소켓을 따로 열면
   출발지 포트가 달라 매핑도 달라져 목적지 의존성과 구분이 안 된다 (NFR-9와 같은 이유).
2. **실제 펀치 시험.** Binding 결과는 매핑 거동만 알려주고 **필터링 거동은 알려주지 않는다.**
   홀펀칭 성립 여부는 둘 다에 달려 있으므로, 두 망 사이에서 최소 펀치 스크립트로 실제 왕복을
   시도해 본다. 엔드포인트는 수동으로 주고받으면 되고 제어 평면은 필요 없다.

1번만 보고 결정하면 매핑은 괜찮은데 필터링 때문에 안 되는 경우를 놓친다.

**선택지.** 어느 것도 현재 M-1/M-4를 그대로 둔 채로는 성립하지 않는다는 점을 먼저 인정해야
한다. 현재 최소 성공은 "두 가정망 사이의 직접 UDP"로 정의되어 있는데, A는 그 토폴로지가
아니고 B는 직접이 아니며 C는 요구된 두 경로를 통과시키지 않는다. **따라서 어느 안을 고르든
성공 기준 자체를 함께 고쳐야 한다.**

| 안 | 내용 | 대가 |
|----|------|------|
| A | NAT 에뮬레이션 테스트베드. 원하는 매핑 거동을 만들어 검증 | 실제 환경 증거가 아니라 분석 설득력이 약해진다. M-4의 "지원 NAT 환경" 정의를 에뮬레이션 포함으로 넓혀야 한다 |
| B | 최소 릴레이를 P1로 승격. direct 실패 시 폴백 | direct-first 주장이 약해지고 Phase가 하나 늘어난다. 폴백 성공을 별도 등급으로 정의해야 한다 |
| C | 모바일 핫스팟 등 제3의 경로를 필수 시험 토폴로지에 추가 | CGNAT이면 더 나쁠 수 있다. 필수 토폴로지 계약을 고쳐야 한다 |

**검증.** 문서에 반영하는 것만으로는 완료가 아니다. 안 C를 골라놓고 그 경로도 실패하면
blocker 20은 그대로 남는다.

- 실측 결과(서버별 관측 엔드포인트, 목적지에 따른 매핑 변화 여부)를 수치로 기록한다
- M-1, M-4와 필수 시험 토폴로지 계약을 고른 안에 맞게 **명시적으로 개정**한다. 개정 없이
  안만 고르면 blocker 20은 그대로 남는다
- 고친 기준 아래에서 성공 경로를 재현한다. 재현 조건은 안마다 다르다

| 안 | 재현 조건 |
|----|-----------|
| A | 에뮬레이터 설정을 문서화하고 그 설정에서 3회 연속 성공. 설정이 곧 통제 조건이다 |
| B | direct 실패를 인위적으로 만든 뒤 폴백 경로로 3회 연속 성공 |
| C | 핫스팟은 통제 대상이 아니다. 통신사, 요금제, 시각, 신호 상태를 기록하고 **그 조건 아래에서** 3회 연속 성공. 조건이 바뀌면 결과가 달라질 수 있음을 함께 적는다 |
- 원래의 "두 가정망 직접 UDP"를 달성하지 못하는 경우, 그것을 실패가 아니라 별도 등급으로
  기록할지 미달로 기록할지 명시한다
- 그 경로가 실패했을 때 무엇이 남는지 적는다. 대비책의 대비책이 없다면 없다고 적는다
- 결정과 근거를 `decisions/`에 ADR로 남긴다

### 5.7 실측 결과 (완료, 2026-09-20)

도구를 만들어 22건을 측정했다. 도구는 [`../../tools/nat-probe/`](../../tools/nat-probe/),
채운 기록과 원본 JSON은 그 아래에 있다(공인 IP 때문에 커밋하지 않는다. 5.9 참조).

| 항목 | 값 |
|------|-----|
| 측정 건수 | 22 |
| 서로 다른 망 | **7곳** (T-Mobile 3, Comcast 3, KT 1) |
| 매핑 판정 | **22건 전부 `endpoint-independent`** |
| 펀치 결과 | **22건 전부 `success`** |
| 대칭형(목적지 의존) NAT | **0건** |
| 시도한 망 조합 | 6쌍. 실패 0쌍 |

| 측정 | 토폴로지 | NAT↔NAT | 다른 ISP | Win↔Win | 요청하지 않은 인바운드 |
|------|----------|:-------:|:--------:|:-------:|------------------------|
| 09-17 | 모바일 핫스팟 2곳 (같은 통신사) | 예 | 아니오 | 아니오 | 미시험 |
| 09-18 | Comcast 가정망 2곳, **3회 연속** | **예** | 아니오 | 아니오 | 미시험 |
| 09-20 | 미국 ↔ 한국, **3회 연속** | 아니오 (KR 공인 IP) | **예** | **예** | 미시험 |
| 09-20 | Comcast 가정망 2곳, Windows | **예** | 아니오 | **예** | **`blocked`** |
| 09-20 | 워싱턴 ↔ 미시간 | **예** | 아니오 | 아니오 | **`blocked`** |
| 09-20 | T-Mobile 핫스팟 ↔ Comcast 가정망 | **예** | **예** | 아니오 | **`blocked`** |

**필수 시험 토폴로지 (2) "서로 다른 가정망 2곳"은 09-18에 3회 연속 성공했다.** 회차 사이에
조건을 바꾸지 않았고 종료 시각이 매 회차 1초 이내로 일치했다.

**세 조건을 한 측정 안에서 동시에 만족한 적은 없다.** 조합으로는 전부 덮였고 시도한 6쌍이
모두 성공했지만, 단일 측정으로 증명한 것은 아니다. 이 한계를 보고서에 싣는다.

### 5.8 부수 발견 — 요청하지 않은 인바운드는 NAT이 막는다

**이 항목은 원래 계획에 없었다.** 09-20 측정에서 Windows 방화벽 프롬프트가 뜨지 않았는데도
인바운드가 전부 들어온 것을 조사하다 드러났다.

인바운드 기본 동작이 `Block` 이고 `python.exe` 허용 규칙도 없는데 펀치는 성립했다. **상태
기반 UDP** 로 설명된다. 우리가 먼저 보냈으므로 응답이 요청된 트래픽으로 분류됐다는 것이다.
다른 설명을 배제한 것은 아니다.
그래서 **내가 보낸 적 없는 출발지 포트에서 오는 패킷**이 시험되지 않은 채 남아 있었다.

`--unsolicited` 시험을 만들어 세 쌍에서 재봤다. **6건 전부 `blocked`** 다.

| 호스트 OS | 호스트 방화벽 | 결과 |
|-----------|---------------|------|
| Windows 11 | 인바운드 기본 `Block`, `python.exe` 규칙 없음 | `blocked` |
| macOS | Application Firewall 기본 꺼짐 | `blocked` |
| Linux | `/etc/ufw/ufw.conf` 가 `ENABLED=no` (직접 확인) | `blocked` |

**호스트 방화벽 필터링이 없다고 보이는 기기에서도 막혔다. 원인은 호스트가 아니라 NAT의
포트 제한 필터링일 가능성이 가장 높다.** macOS는 기본값 추론이고 Linux는 `ufw` 만 확인했으므로 단정하지는
않는다. 관측이 포트 제한 NAT과 일치할 뿐이다.

두 가지가 따라온다.

- **클라이언트가 방화벽 규칙을 등록하지 않는다. 잠정 결정이다.** 원인이 NAT이면 해도
  해결되지 않는다. 현재로서는 관리자 권한 요구사항이 생기지 않고 `spec.md`의 "수동 포트
  포워딩 없이" 주장이 유지된다. **다만 우리 Windows가 받는 방향에서는 방화벽이 원인일
  가능성이 배제되지 않았다.** Windows 한 대에 범위를 좁힌 임시 인바운드 규칙을 넣고
  `--unsolicited` 를 다시 돌리면 갈린다. 결정을 뒤집는 조건은 [ADR 0002](decisions/0002-리바인딩-복구-미보장.md)에 있다
- **[`protocol.md`](protocol.md) 10.4 (c) 경로 검증의 전제가 깨진다.** 10.4가 지명 대신
  학습을 고른 세 이유 중 **NAT 리바인딩 복구**가, 시험한 세 망에서 관측된 필터링 아래에서는 보장되지 않는다.
  [ADR 0002](decisions/0002-리바인딩-복구-미보장.md)로 결정을 남기고 `protocol.md`에 반영했다

### 5.9 결론과 남은 조치

**blocker 20 해소.** 대비책을 도입하지 않고 M-1/M-4를 현재 문구 그대로 유지한다.
선택지 A(에뮬레이션), B(릴레이 P1 승격), C(핫스팟 필수화)를 모두 기각했다.
근거와 받아들인 위험은 [ADR 0001](decisions/0001-직접-연결-대비책-미도입.md)에 있다.

**결정 2건.**

| 항목 | 결정 |
|------|------|
| 실측 원본 공개 여부 | **로컬 보관.** `tools/nat-probe/results/` 와 `records/` 를 `.gitignore` 에 넣었다. 이 레포는 public 이고 두 가정의 공인 IP와 분 단위 시각이 들어 있다. 친구 집 IP를 공개하는 것은 되돌릴 수 없고 본인 동의도 없다. 첨부는 원본 그대로 하지 않는다. 제출 시스템과 공유 링크에도 같은 노출이 생긴다. **공인 주소는 되돌릴 수 없는 가명(`망 A`, `망 B`)으로 바꾸고 시각은 날짜 단위까지만 남긴다.** IPv4 마지막 옥텟만 가리는 것으로는 부족하고 IPv6에도 적용되지 않는다. 그렇게 하지 않으려면 당사자 동의를 명시적으로 받는다. 어느 쪽을 택했는지 보고서에 적는다. **대가:** 기기를 옮기면 원본이 따라가지 않는다. 판정과 근거는 이 문서와 ADR에 남으므로 결론은 유실되지 않는다 |
| ADR 작성 | **완료.** `decisions/` 에 0001, 0002 |

**남은 조치.**

1. ~~Phase 8 준비 절차에 "시연 직전 재측정"을 넣는다~~ **완료.** `roadmap.md` Phase 8 작업에
   추가했다. 이 결정은 특정 시점의 관측에 기대고 있고 공유기 교체나 ISP 구성 변경으로 무효가
   될 수 있다. 대비책의 대비책은 없다
2. Phase 4 keepalive 실측에서 매핑 수명을 재고, 15s 간격이 리바인딩을 실제로 막는지 확인한다
3. 보고서에 증거의 한계를 그대로 싣는다. 세 조건 동시 만족 측정이 없다는 것, 표본이 Comcast에
   치우쳐 있다는 것, 다른 ISP 조합의 한쪽이 핫스팟이라는 것
---

## 6. 과잉 검증 기준 되돌리기

네 항목이 같은 문제는 아니다. 하나는 정상 구현이 통과할 수 없는 기준이고(재발 방지 규칙 3번
위반), 둘은 측정 방법이 없어 판정 자체가 불가능하며, 하나는 증거로서 타당하지 않다.
구분해서 다룬다.

| 다룰 것 | 성격 | 근거 |
|---------|------|------|
| Phase 7 "송수신 바이트 카운터가 패킷 캡처와 정확히 일치, 오차 불허". 캡처 지점 위치, NIC 오프로드, 캡처 자체의 유실 때문에 정상 구현도 정확히 일치하지 않는다 | 통과 불가 | warn |
| Phase 8 게임 지연 ±30ms. 측정 방법과 계측 지점이 없어 판정 자체가 불가능하다 | 미정 | warn |
| A-2의 통제된 실패 시나리오(UDP 차단, 없는 엔드포인트)가 필수 사례로 고정. 인위적 실패가 실제 실패 분포의 증거가 되지 못한다 | 증거 타당성 | warn |
| Phase 9 통계 설계 미정. 시행 시간, 독립성, 불확실성 보고 | 미정 | warn |

**검증.** "정상 구현이 통과하는가"는 출발 질문이지 판정 기준이 아니다. 성격별로 완료 조건이
다르다.

**정량 기준**(Phase 7 카운터, Phase 8 지연, Phase 9 통계)은 아래 넷을 갖춰야 한다.

| 갖출 것 | 뜻 |
|---------|-----|
| 대상 문서와 절 | 어느 기준을 고쳤는지 |
| 측정 절차 | 무엇을 어떻게 재는지. 도구와 계측 지점 포함 |
| 허용 오차와 표본 조건 | 숫자. 표본 수와 통계량(중앙값 등)까지 |
| 기대 결과 | 어떤 값이 나오면 통과인지 |

**정성 기준**(A-2)은 숫자를 요구하지 않는다. 대신 그 시나리오가 무엇의 증거이고 무엇의
증거가 아닌지를 적는다. 인위적 실패는 실패 처리 경로가 동작한다는 증거이지, 실제 환경에서의
실패율에 대한 증거가 아니다. 이 구분을 쓰지 않으면 A-2는 다시 잘못된 기준이 된다.

기준을 낮출 때는 **무엇을 더 이상 보장하지 않는지** 같이 적는다. 느슨해진 만큼 진짜 결함도
같이 통과하게 되므로, 그 범위를 명시하지 않으면 기준을 없앤 것과 같다.

---

## 부채

| 항목 | 내용 |
|------|------|
| `decisions/` 비어 있음 | 단일 이벤트 루프는 `architecture.md` 3.2, 엔드포인트 학습과 20바이트 헤더는 `protocol.md`, 릴레이 범위는 `spec.md`/`roadmap.md`에 결과가 적혀 있다. 하지만 **왜 그 대안을 버렸는지**를 담은 전용 ADR이 없다 |
| `experiments.md` 없음 | Phase 9 산출물. 현재 유일하게 깨진 상대 링크 |
| `31e4240` 기록 없음 | CMake 스모크 커밋. `commit_history/` 항목 미작성 |
