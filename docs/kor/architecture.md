# Architecture

**프로젝트:** Direct-First P2P Virtual Network for Multiplayer Games
**대상 플랫폼:** Windows 10 / 11 x64 (클라이언트), Linux/AWS EC2 (제어 서버)

> English version: [`../eng/architecture.md`](../eng/architecture.md)

---

## 1. 설계 원칙

| # | 원칙 | 의미 |
|---|------|------|
| 1 | Direct-First | 게임 트래픽은 가능한 한 피어 간 직접 경로로 흐른다. AWS는 조율 전용이며 데이터 경로가 아니다. |
| 2 | 평면 분리 | 제어 평면(Python/AWS)과 데이터 평면(C++/클라이언트)은 서로 독립적으로 동작하고 테스트된다. |
| 3 | 게임 비종속 | 터널 계층에 Minecraft 전용 로직을 넣지 않는다. Minecraft는 검증 대상일 뿐이다. |
| 4 | 관측 가능 | 모든 연결 시도는 성공/실패와 **실패 단계**를 기록한다. |
| 5 | 의존성 최소화 | Wintun은 가상 인터페이스 접근만 담당한다. STUN, NAT 통과, 터널, 라우팅은 직접 구현한다. |
| 6 | 위험 우선 | 가장 불확실한 것(NAT 통과)을 가장 먼저 검증한다. 가상 어댑터는 그 뒤다. |

---

## 2. 시스템 구성

```text
             AWS (EC2 1 인스턴스, 프로세스 2개, 포트 분리)
   +---------------------------+     +--------------------------+
   | Python Coordination Server|     | Python Telemetry Service |
   |  rooms / peers            |     |  telemetry ingest        |
   |  candidate exchange       |     |  connection results      |
   +-------------+-------------+     +------------+-------------+
                 |                                |
                 +------- Amazon DynamoDB --------+
                 |                                |
      조율 트래픽 (TCP/JSON)             지표 업로드 (TCP/JSON)
                 |                                |
                 +----------------+---------------+
                                  |
              +-------------------+-------------------+
              |                                       |
      +-------v---------+                    +--------v--------+
      | Windows Client A|                    | Windows Client B|
      |  Wintun Adapter |                    |  Wintun Adapter |
      |  Router         |                    |  Router         |
      |  Tunnel         |<==================>|  Tunnel         |
      |  Hole Punch     |   Direct P2P UDP   |  Hole Punch     |
      |  STUN Client    |   (게임 트래픽)      |  STUN Client    |
      |  Telemetry      |                    |  Telemetry      |
      +-------+---------+                    +--------+--------+
              |                                       |
      Minecraft Server                         Minecraft Client
        10.100.0.1:25565                          10.100.0.2
```

제어 평면이 죽어도 **이미 수립된 P2P 터널은 계속 동작**해야 한다. 제어 평면은 세션
수립에만 필요하다.

**텔레메트리 수집은 제어 평면이 맡지 않는다.** 별개 서비스이고 배포 단위가 다르다. 두
서비스는 같은 EC2 인스턴스에 서로 다른 프로세스로 뜨지만 의존은 한 방향도 없다. 경계
계약은 3.4 텔레메트리 서비스가 갖는다.

---

## 3. 컴포넌트

### 3.1 클라이언트 (C++20)

| 모듈 | 책임 | 외부 의존 |
|------|------|-----------|
| `network/udp_socket` | Winsock2 초기화/해제, UDP 소켓 생성, 논블로킹 송수신, 이벤트 핸들 노출 (3.2 동시성 모델) | Winsock2 |
| `network/endpoint` | `IP:Port` 값 타입, 파싱, 비교, 직렬화 | 없음 |
| `network/stun_client` | STUN Binding Request 생성, 트랜잭션 ID 관리, 응답 파싱, `XOR-MAPPED-ADDRESS` 디코드, 타임아웃/재시도 | 없음 (RFC 5389 직접 구현) |
| `peer/peer` | 피어 식별자, 가상 IP, 후보 엔드포인트 목록 | 없음 |
| `peer/hole_punch` | 양방향 동시 송신, 재시도, 성공 판정, 실패 사유 분류 | 없음 |
| `peer/session` | 핸드셰이크, keepalive, 연결 상태 머신, RTT 측정 | 없음 |
| `tunnel/packet` | 터널 헤더 직렬화/역직렬화, 손상 패킷 검증 | 없음 |
| `tunnel/tunnel` | 패킷 타입별 디스패치, 캡슐화/역캡슐화, 시퀀스 관리 | 없음 |
| `tunnel/router` | 가상 IP -> 피어 세션 매핑, 목적지 결정 | 없음 |
| `adapter/wintun_adapter` | 가상 어댑터 생성/개방과 패킷 read/inject (Wintun), 가상 IP 주소 및 라우트 설정 (IP Helper) | **Wintun (승인 완료)**, IP Helper |
| `telemetry/telemetry` | 지표 수집, 로컬 버퍼링, 텔레메트리 서비스 전송 | 없음 |
| `control/control_client` | 제어 평면 HTTP/JSON 호출과 DNS 해석. `[control]` 스레드가 소유한다 (3.2.8 `[control]` 스레드). 연산과 인코딩은 [`control_plane.md`](control_plane.md) | Winsock2 |

클라이언트는 단일 프로세스다. 스레드 구성과 상태 소유는 3.2 동시성 모델에 있다.

**모든 UDP 송수신은 하나의 로컬 소켓을 공유하고, 그 소켓은 하나의 수신 루프가 배타적으로
소유한다.** 이 제약은 Phase 2 시점부터 지켜야 한다.

> **왜.** 서로 다른 소켓을 쓰면 NAT가 각각 다른 공인 포트를 매핑하므로 STUN으로 발견한
> 엔드포인트가 터널에 적용되지 않는다.

소켓을 공유한다는 것은 STUN 응답과 터널 패킷이 같은 큐로 들어온다는 뜻이다. STUN
클라이언트가 직접 `recvfrom`을 부르면 두 읽기가 서로의 패킷을 훔친다. STUN 클라이언트는
요청만 등록하고 응답은 수신 루프에서 전달받는다. 분류 규칙과 필수 소켓
옵션(`SO_EXCLUSIVEADDRUSE`, `SIO_UDP_CONNRESET` off, `connect()` 미호출)은
[`protocol.md`](protocol.md) 6장 소켓 소유권과 7장 수신 분류에 있다.

### 3.2 동시성 모델

**터널 상태를 건드리는 모든 코드는 한 스레드에서만 돈다.** 잠금을 걸어 공유하는 대신
공유하지 않는다.

`[main]` / `[net_rx]` / `[tun_rx]` / `[timer]` 4스레드로 나누지 않는다. 이유는 둘이다.

- 네 스레드가 세션 상태와 송신 `sequence`를 동기화 없이 변경한다
- 텔레메트리 업로드가 타이머 스레드를 공유해 **업로드 대상 장애가** keepalive를 멈춘다.
  이쪽은 NFR-3을 설계 단계에서 위반한다

#### 3.2.1 스레드

| 스레드 | 책임 | 공유 상태 |
|--------|------|-----------|
| `[loop]` | UDP 수신, Wintun 수신, 콘솔 명령, 타이머, 세션 상태, 라우팅, 모든 송신 | **터널 상태 없음.** 큐 네 개(텔레메트리, 콘솔, 제어 요청, 제어 응답)의 한쪽 끝과 종료/정리 이벤트만 공유한다 |
| `[telemetry]` | 지표 큐 소비, 텔레메트리 서비스 업로드. Phase 9 이후 | 큐 하나 |
| `[console]` | 표준 입력 읽기. Phase 1~5 스캐폴딩 | 명령 큐 하나 |
| `[control]` | 제어 평면 TCP 호출, DNS 해석. Phase 3 이후 | 요청 큐와 응답 큐 하나씩 (3.2.8 `[control]` 스레드) |

**`[loop]`가 프로세스 주 스레드다.** 나머지는 그 구간에 그 일이 있을 때만 있다.

| 구간 | 스레드 |
|------|--------|
| Phase 1~2 | `[loop]`, `[console]` |
| Phase 3~5 | `[loop]`, `[console]`, `[control]` |
| Phase 6~8 | `[loop]`, `[control]` |
| Phase 9 이후 | `[loop]`, `[control]`, `[telemetry]` |

- `[console]`은 Phase 1~5에만 있다. `[loop]`가 표준 입력에서 블록할 수 없으므로 별도
  스레드가 읽어 큐에 넣는다
- `[telemetry]`가 늦게 오는 이유는 올릴 곳이 그때 생기기 때문이다. 텔레메트리 서비스와
  업로드 스레드가 같은 Phase 의 산출물이다 ([`roadmap.md`](roadmap.md))
- Phase 6에서 어댑터가 들어올 때는 Wintun 읽기 이벤트가 `[loop]`의 대기 집합에 직접
  합류하므로 스레드가 늘지 않는다

#### 3.2.2 대기

```text
WaitForMultipleObjects(n, handles, FALSE, timeout_ms)
```

| 순위 | 핸들 | 생성 | 유효 구간 |
|------|------|------|-----------|
| 0 | 종료 이벤트 | `CreateEvent` (수동 리셋) | 항상 |
| 1 | UDP 소켓 이벤트 | `WSACreateEvent` + `WSAEventSelect(sock, ev, FD_READ)` | 항상 |
| 2 | Wintun 읽기 이벤트 | `WintunGetReadWaitEvent(session)` | Phase 6 이후 |
| 3 | 콘솔 명령 이벤트 | `CreateEvent` (자동 리셋). `[console]`이 신호 | Phase 1~5 |
| 4 | 제어 응답 이벤트 | `CreateEvent` (자동 리셋). `[control]`이 응답 큐에 넣은 뒤 신호 | Phase 3 이후 |

**위 번호는 논리적 순위이지 배열 인덱스가 아니다.** `WaitForMultipleObjects`는 유효한
핸들이 빈틈없이 채워진 배열을 요구한다. 빈자리에 `NULL`을 넣으면 `WAIT_FAILED`가 난다.
구성이 바뀌는 시점마다 살아 있는 핸들만 모아 조밀한 배열을 만들고, 논리적 출처에서 실제
인덱스로 가는 대응표를 따로 둔다.

| 구간 | 배열 길이 | 들고 나는 핸들 |
|------|:---:|-----|
| Phase 1~2 | 3 | |
| Phase 3~5 | 4 | 제어 응답 이벤트가 들어온다 |
| Phase 6 이후 | 4 | 콘솔 이벤트가 빠지고 Wintun 이벤트가 들어온다 |

`timeout_ms`는 아래처럼 계산한다. **뺄셈을 먼저 하면 안 된다.**

> **왜.** `GetTickCount64()`는 부호 없는 값이라 마감이 이미 지났을 때 뺄셈이 언더플로해
> 거대한 값이 되고, `DWORD`로 좁히면 `0xFFFFFFFF`, 즉 `INFINITE`에 착지할 수 있다.
> 그러면 루프가 영원히 깨지 않는다.

```text
next_timeout:
    if 대기 중인 타이머 없음:  return INFINITE
    t = now()                              # 한 번만 읽는다
    d = next_deadline()
    if d <= t:                return 0
    return (DWORD) min(d - t, INFINITE - 1)
```

`now()`를 두 번 부르면 안 된다.

> **왜.** 비교와 뺄셈 사이에 시계가 `d`를 지나가면 언더플로해서 타임아웃이 약 49.7일이
> 되고, 루프가 사실상 멈춘다.

`WSAPoll`을 쓰지 않는 이유는 Wintun 읽기 대기가 소켓이 아니라 Win32 이벤트 핸들이기
때문이다. 소켓과 이벤트 핸들을 한 번의 대기에 섞으려면 `WaitForMultipleObjects`여야 한다.
`WSAEventSelect`는 소켓을 자동으로 논블로킹 모드로 바꾸므로 `ioctlsocket(FIONBIO)`를 따로
부르지 않는다.

#### 3.2.3 루프 한 바퀴

```text
busy = false
loop:
    r = WaitForMultipleObjects(n, handles, FALSE, busy ? 0 : next_timeout())

    if r == WAIT_FAILED:                      # 복구 불가
        로그 후 shutdown(); break             # 정상 경로와 같은 정리를 거친다
    if r == WAIT_OBJECT_0 + idx(SHUTDOWN):
        shutdown(); break

    WSAEnumNetworkEvents(sock, udp_ev, &ne)   # 소켓 이벤트를 리셋한다. 필수
    u = drain_udp(MAX_DRAIN)                  # r로 분기하지 않는다
    w = drain_wintun(MAX_DRAIN)
    if w == RESTART:
        어댑터 세션 재생성 -> 실패하면 shutdown(); break
        성공하면 살아 있는 핸들로 배열과 인덱스 대응표 재구성
    drain_console()
    drain_control()                           # 3.2.8. Phase 3 이후
    run_expired_timers(now())
    busy = (u == BUDGET) or (w == BUDGET)
```

`WAIT_FAILED`도 `shutdown()`을 거친다. 여기서 바로 빠져나가면 `CLOSE` 송신과 어댑터 정리를 건너뛰어 상대는 50초 동안 죽은 세션을 붙들고 어댑터와 라우트가 남는다. **비정상 종료야말로 정리가 필요한 경우다.**

**`WSAEnumNetworkEvents`를 반드시 부른다.** 이 호출이 이벤트 리셋과 네트워크 이벤트 조회를
한 번에 처리한다.

> **왜.** `WSAEventSelect`가 만드는 이벤트는 수동 리셋이고, `recvfrom`은 `FD_READ` 통지를
> 재무장할 뿐 이미 신호된 이벤트 객체를 리셋하지 않는다. 이 호출을 빠뜨리면 첫 데이터그램
> 이후 이벤트가 계속 신호 상태로 남아 `WaitForMultipleObjects`가 매번 즉시 반환하고, 루프가
> CPU를 태우며 스핀한다.

**`r`로 분기하지 않는다.** 매 바퀴 양쪽을 모두 비우면 인덱스 순서와 무관해지고, 비어 있을
때의 비용은 `WSAEWOULDBLOCK` 하나와 `ERROR_NO_MORE_ITEMS` 하나뿐이다.

> **왜.** `bWaitAll = FALSE`인 `WaitForMultipleObjects`는 신호된 핸들 중 가장 낮은 인덱스
> 하나만 반환한다. 반환값으로 분기하면 UDP 이벤트가 계속 신호되는 동안 그보다 뒤에 놓인
> Wintun 이벤트가 굶는다. 게임 트래픽이 많을 때가 정확히 그 상황이다.

**두 drain은 예산을 가진다.** `MAX_DRAIN`은 한 바퀴당 소스별 64개다. 1472바이트 기준
94KB이고 파싱과 송신 비용이 1ms 미만이라, 가장 짧은 타이머(200ms `HELLO` 재전송) 대비
무시할 수 있다. 예산에 걸려 멈추면 `busy`가 참이 되어 다음 대기가 타임아웃 0으로 즉시
반환한다. 남은 데이터는 다음 바퀴에 처리되고 **그 사이에 타이머가 한 번 돈다.**

> **왜.** 예산 없이 비어질 때까지 돌면, 도착 속도가 처리 속도보다 빠를 때 `drain_udp`가
> 영원히 반환하지 않고 타이머가 한 번도 돌지 않는다. keepalive와 `HELLO` 재전송이 멈추고,
> 바로 위에서 세운 "수신 먼저, 타이머 나중" 순서가 "타이머는 절대 안 돈다"로 변한다.

```text
drain_udp(budget) -> {EMPTY, BUDGET}:
    budget회 반복:
        n = recvfrom(sock, buf, MAX_DATAGRAM + 1, &src)   # 버퍼는 1473바이트
        if n < 0:
            e = WSAGetLastError()
            if e == WSAEWOULDBLOCK:  return EMPTY      # 소스가 비었다
            if e == WSAEMSGSIZE:                       # 1473바이트로도 모자랐다
                drop_oversize_datagram++; continue     # 데이터그램은 이미 소비됐다
            카운터 증가 후 return EMPTY                 # 소켓은 계속 사용한다
        if n > MAX_DATAGRAM:                           # 정확히 1473바이트가 왔다
            drop_oversize_datagram++; continue
        classify(buf, n, src)                          # protocol.md 7장
    return BUDGET                                      # 예산 소진. 아직 남았다
```

**과대 데이터그램에서 비우기를 멈추지 않는다.** `MAX_DATAGRAM`(1472)을 넘는 데이터그램도
반환되는 시점에는 이미 소켓 큐에서 사라진 상태다. 거기서 `return EMPTY` 하면 그 바퀴의
비우기가 한 개에서 끝나므로, 과대 데이터그램을 섞어 보내는 것만으로 배칭이 무력화된다.
버리고 계속 비운다. 다른 오류는 원인을 모르므로 그 바퀴를 끝낸다.

**수신 버퍼는 `MAX_DATAGRAM + 1`(1473)바이트다.** 1바이트 크게 잡으면 `n > MAX_DATAGRAM`
이라는 길이 비교가 판정이 되고, 시험에서 1473바이트를 보내 이 경로를 결정적으로 재현할 수
있다. 버퍼를 `MAX_DATAGRAM` 크기로 잡으면 과대 패킷을 `WSAEMSGSIZE`라는 오류 코드로만 알 수
있고, 그 판정이 Winsock의 절단 동작에 의존한다.

**그래도 `WSAEMSGSIZE` 분기를 지우지 않는다.** 1473바이트를 넘는 데이터그램은 여전히 그
오류로 올라온다. 두 자리는 같은 사건이므로 카운터도 같은 것을 쓴다.

`FD_READ`는 레벨 성격이다. `recvfrom` 후에도 데이터가 남아 있으면 이벤트가 다시 신호되므로,
한 바퀴에 한 개만 읽어도 데이터그램을 잃지는 않는다. **비우는 이유는 정확성이 아니라
비용이다.** 데이터그램마다 대기와 `WSAEnumNetworkEvents`를 왕복하면 깨어남 횟수가 수신 패킷
수만큼 늘고 타이머 처리도 그만큼 밀린다. 정확성 요건은 위의 `WSAEnumNetworkEvents`
호출이고, 비우기는 그 위에 얹는 배칭 정책이다.

```text
drain_wintun(budget) -> {EMPTY, BUDGET, RESTART}:
    if session == NULL:  return EMPTY                  # Phase 1~5. 어댑터가 없다
    budget회 반복:
        p = WintunReceivePacket(session, &size)
        if p == NULL:
            e = GetLastError()
            ERROR_NO_MORE_ITEMS -> return EMPTY         # 링이 비었다. 정상
            ERROR_HANDLE_EOF    -> 종료 이벤트 신호 후 return EMPTY
            ERROR_INVALID_DATA  -> return RESTART       # 링 손상. 아래 참조
            그 외                -> 카운터 증가 후 return EMPTY
        guard = 스코프 이탈 시 WintunReleaseReceivePacket(session, p)
        route_and_send(p, size)
    return BUDGET
```

`WintunReleaseReceivePacket`은 **스코프 가드로 건다.** 호출 순서상 `route_and_send` 뒤에
두면 그 안에서 조기 반환하거나 예외가 나갈 때 링 패킷이 샌다. 한 번 빠뜨릴 때마다 링 버퍼가
영구히 그만큼 줄고 결국 수신이 멈춘다.

`RESTART`는 `[loop]`가 처리한다. 링이 손상되면 세션을 새로 만들어야 하는데, **새 세션은 새
읽기 이벤트 핸들을 준다.** 따라서 세션 재생성과 대기 배열 재구성이 한 묶음이다.
`drain_wintun` 안에서 재시작하면 자기가 방금 무효화한 핸들을 호출자가 계속 기다리게 된다.

재생성이 실패하면 어댑터 없이 계속 도는 대신 `shutdown()`으로 간다. 배열은 조밀하게 다시
만들어지므로 Wintun 핸들이 그냥 빠질 뿐 대기 자체는 정상 동작한다. 그래서 더 위험하다.
**터널이 죽은 채 살아 있는 척한다.** 세션은 `CONNECTED`로 남고 keepalive도 계속 나가지만
게임 패킷은 한 개도 흐르지 않는다.

**주입 실패는 폐기한다.** `DATA`가 [`protocol.md`](protocol.md) 8.4 `DATA` 내부 검증을
통과하면 `WintunAllocateSendPacket`으로 송신 링의 자리를 잡고 `WintunSendPacket`으로 넣는다.
송신 링에 자리가 없어 할당이 실패하는 것은 고장이 아니라 부하가 높을 때 정상적으로 일어나는
일이다. 그 패킷을 버리고 `drop_inject_error`를 올린다.

**재시도하지 않는다.**

> **왜.** 이 스레드가 타이머와 두 수신 경로를 같이 책임지므로, 자리가 날 때까지 기다리면
> 그동안 keepalive와 `HELLO` 재전송이 멈춘다. 링이 찰 만큼 소비가 밀린 상태에서는 그 자리에서
> 다시 시도해도 같은 곳에서 또 막힌다.

버리는 대가는 내부 패킷 한 개다. **그 대가가 회복되는지는 내부 프로토콜에 달렸다.** 내부가
TCP면 재전송이 메우므로 지연이고(4장 데이터 평면 경로의 Minecraft 경로가 그렇다), UDP 같은
비신뢰 프로토콜이면 그 패킷은 그대로 잃는다. 터널은 내부를 구분하지 않으므로(NFR-2) 이것을
"항상 지연일 뿐"이라고 적지 않는다.

**지금 확정하는 것은 카운터 하나와 동작 계약 넷이다.** 카운터는 `drop_inject_error` 하나다.
동작은 **주입이 실패하면 그 패킷을 버리고, 카운터를 올리고, 재시도하지 않고, 루프를 멈추지
않는다.** 이 넷은 어떤 API 모양에서도 성립한다.

**어댑터가 없는 빌드(Phase 4~5)에서 8.4 `DATA` 내부 검증을 통과한 `DATA`가 갈 곳도 여기서
정한다.** 그 구간에는 주입할 어댑터가 없으므로 그 절이 가리키는 "Wintun 주입 직전" 이
가리키는 다음 단계가 비어 있다.

| 빌드 | 8.4 통과 `DATA` |
|------|------------------|
| 어댑터 있음 (Phase 6 이후) | 위의 주입 경로 |
| 어댑터 없음, 시험 빌드 | **페이로드 훅**에 넘긴다. 훅은 꺼낸 페이로드 바이트열과 길이를 그대로 받는 함수 하나이고, [`roadmap.md`](roadmap.md) Phase 4 의 M-5 byte-for-byte 비교와 에코 응답기가 그 자리에서 붙는다 |
| 어댑터 없음, 제품 빌드 | 버리고 `drop_no_sink`를 올린다 |

**훅을 시험 전용 경로로 따로 짜지 않는다.** 8.1~8.4 검증을 통과한 뒤의 같은 지점에 걸어야 그
시험이 실제 수신 경로를 검증한다. 7장 출발지 위생 시험이 수신 진입점을 하나로 못박은 것과
같은 이유이고, 여기는 그 대칭인 출구다. `drop_no_sink`는 제품 빌드에서 0이 아니면 설계가
어긋난 것이므로 그 자체가 진단이다.

**"링이 찬 것"을 별도 카운터로 나누는 것은 확인한 뒤에 한다.** 실패가 어느 호출에서 어떤
형태로(반환값인지 `GetLastError`인지, 애초에 보고되는지) 나오는지는 쓰는 Wintun 헤더와 그
문서가 정한다. 이 레포에는 그 헤더가 없고 확인하지 않았다. 확인한 뒤에 이 절에 세부 카운터를
추가한다. 언제 확인하는지는 `roadmap.md`가 갖는다.

> **왜.** 확인하지 않은 API 모양을 여기 적으면 구현이 그것을 그대로 믿는다.

`drain_console`은 `[console]`이 큐에 넣은 명령 줄을 비운다. 이벤트가 자동 리셋이라 별도 리셋
호출이 필요 없다. Phase 6 이후에는 이 핸들도 스레드도 사라진다.

**`quit` 명령은 종료 이벤트를 신호한다.** `shutdown()`을 직접 부르지 않는다. 그러면
`[telemetry]`와 `[control]`이 같은 신호로 함께 깨어나고, 다음 바퀴의 대기가 종료 핸들에서
반환해 3.2.7 종료의 순서를 그대로 탄다. 3.2.7 (1)이 "아직 안 됐으면"이라고 적은 것이 이
경로다.

**콘솔이 만든 송신 잔량은 `busy`에 포함한다.** `send <n>`(Phase 4~5)과 `raw`(3.5 기동 입력)는
한 바퀴에 몰아넣지 않고 `MAX_DRAIN`과 같은 방식으로 나눠 보내므로, 잔량이 남은 바퀴는
`busy = true`여야 다음 대기가 타임아웃 0으로 즉시 돌아온다. 그렇지 않으면 남은 송신이 타이머
간격만큼씩 늘어진다.

`drain_control`은 `[control]`이 응답 큐에 넣은 결과를 비우고 세션 상태에 반영한다(3.2.8
`[control]` 스레드). 두 큐는 작고 요청이 사람 속도이므로 예산을 두지 않는다.

Wintun 문서는 부하가 높을 때 `ERROR_NO_MORE_ITEMS`에서 잠시 스핀한 뒤 이벤트를 기다리라고
권한다. **스핀하지 않는다.** 이 루프는 타이머도 책임지므로 스핀이 keepalive와 `HELLO`
재전송을 그만큼 지연시킨다. 스핀은 처리량 최적화이고 우리 목표는 지연(NFR-1)이다. Phase 7
실측에서 수신이 병목으로 나오면 그때 재검토한다.

**어댑터 읽기는 `adapter_rx`로 센다.** `WintunReceivePacket`이 패킷 하나를 돌려줄 때마다
오른다. Phase 6 검증이 "어댑터 read 카운터" 로 읽는 값이 이것이다(`roadmap.md`). 주입 쪽은
성공을 세지 않는다. 실패만 `drop_inject_error`로 센다.

**이 절의 카운터는 `protocol.md`의 카운터와 계층이 다르다.** `drop_oversize_datagram`,
`drop_inject_error`, `drop_no_sink`, `adapter_rx`는 소켓과 어댑터 API가 낸 결과를 세는 것이라
이 문서가 정의한다. `protocol.md` 7장 수신 분류와 8장 수신 검증 파이프라인의 `drop_*`는 와이어
분류와 검증 파이프라인의 카운터다. 목록을 만들 때 두 출처를 섞지 않는다.

#### 3.2.4 타이머

- 단조 시계는 `GetTickCount64()`를 쓴다. 밀리초 단위 64비트라 실질적으로 랩어라운드가
  없다(약 5.8억 년). RTT 측정만 `QueryPerformanceCounter`를 쓰고, **두 시계의 값을 서로
  비교하지 않는다.**
- 타이머 집합이 작다. [`protocol.md`](protocol.md) 11장 타이머의 표가 한 자릿수이고 최소
  범위에서 세션은 하나다. 매 바퀴 선형 주사로 가장 이른 마감을 구한다. 타이머 휠은 필요 없다.
  - 같은 장의 "만료 값" 표(폐기 목록, nonce, probe, `pending_pings`)는 세션 타이머가 아니라
    **항목마다 붙는 수명**이라 그 항목을 훑을 때 함께 본다
- `protocol.md` 11장 타이머는 마감 비교에 엄격 부등호를 쓰고, 우선순위를 꺼낸 시각 기준으로
  정의한다. 같은 바퀴에 꺼낸 수신 이벤트가 그 바퀴의 타이머보다 먼저 처리된다. **수신을 먼저
  비우고 타이머를 나중에 도는 이 루프 순서가 그 규칙의 구현이다.**
  - 도착 시각 기준이 아닌 이유는 drain 예산 때문이고, 그 근거는 `protocol.md` 11장에 있다

#### 3.2.5 상태 소유

다음은 전부 `[loop]`가 단독 소유한다. 뮤텍스도 원자 변수도 쓰지 않는다. 필요가 없어서다.

- 세션 상태 머신, `session_epoch`, 송신 `sequence`, 재생 방지 비트맵, `peer_endpoint`,
  마지막 수신 시각, 후보 목록, `pending_pings`, 모든 폐기 카운터
- **로컬 기록 파일의 핸들과 쓰기** (9장 텔레메트리와 기록)
- `room_id`·`peer_id`·`peer_token`·상대 피어 정보 같은 제어 평면에서 받은 값. `[control]`은
  그 값을 응답 큐로 넘길 뿐 자기 상태로 들고 있지 않다. 예외는 3.2.8 `[control]` 스레드가
  적은 해석된 서버 주소 하나다

**송신 `sequence` 증가는 반드시 `[loop]`에서만 일어난다.** `DATA`와 `KEEPALIVE`가 같은
카운터를 쓰므로 두 스레드에서 증가시키면 같은 번호를 단 패킷 두 개가 나간다. 받는 쪽 중복
억제([`protocol.md`](protocol.md) 4.5 중복 억제와 손실 집계)는 뒤엣것을 정확히 버린다. 자기가
만든 위조 중복이다.

어떤 구조체에도 "스레드 안전"이라는 주석을 붙이지 않는다. 단일 소유가 유일한 규칙이고, 예외를 하나 허용하는 순간 규칙이 사라진다.

#### 3.2.6 텔레메트리 격리

`[telemetry]`가 별도 스레드인 이유는 병렬성이 아니라 **격리**다.

- `[loop]`는 고정 크기 SPSC 링 버퍼에 레코드를 넣는다. 생산자 하나, 소비자 하나뿐이라 원자
  head/tail만으로 **락 없이** 구현된다
- 가득 차면 새 레코드를 버리고 `telemetry_queue_dropped` 를 올린다. 대기 없음, 실패 보고 없음
- `[telemetry]`가 큐를 비워 텔레메트리 서비스에 올린다. 업로드가 블록되든 실패하든 `[loop]`와
  무관하다
- 텔레메트리는 **텔레메트리 서비스로 가는 자체 TCP 소켓**을 쓴다. 제어 평면 소켓과 공유하지
  않는다. [`protocol.md`](protocol.md) 6장 소켓 소유권의 단일 소켓 제약은 UDP 데이터 평면에만
  적용된다
- **데이터 평면에 락이 하나도 없다.**

**링은 256 항목이다.** 근거는 자동 주기 지표의 배경 생산 속도다. `PING` 5초, keepalive
15초처럼 주기가 정해진 것만 보면 평균 1건/초 미만이고, 그 속도에서 256칸은 분 단위 분량이라
업로드 하나가 소켓 타임아웃(3.2.7 종료)에 걸려 소비가 몇 초 밀리는 구간을 덮는다.

**버스트는 이 크기가 보장하지 않는다.** 시험이 유발하는 연속 연결 시도, 수동 RTT 표본, 폐기
카운터가 몰리는 구간은 같은 초에 여러 건을 만들 수 있고 그러면 더 빨리 찬다. 그때는 새
레코드가 버려진다. 그것이 의도한 동작이다.

> **왜.** 지표는 유실을 허용하는 물건이고, 버스트를 덮으려고 크기를 올리면 장애 구간에서
> 오래된 지표를 더 오래 붙들 뿐이다.

**콘솔 명령 큐는 16 줄이고 같은 정책을 쓴다.** 가득 차면 새 줄을 버리고
`console_queue_dropped` 를 올린다. 생산자가 `[console]`, 소비자가 `[loop]` 하나씩이라 여기도
락이 필요 없다.

- **이 카운터만 3.2.5 상태 소유의 단독 소유 규칙에서 예외다.** 버리는 쪽이 생산자인
  `[console]` 이라 그 스레드가 올린다. 원자 변수 하나이고 `[loop]` 는 읽기만 한다(9장의
  `counter` 이벤트). 다른 폐기 카운터는 전부 `[loop]` 가 올린다
- **두 큐의 카운터를 같은 이름으로 쓰지 않는다.** 이름이 없으면 Phase 1 검증이 무엇을 보는지
  정할 수 없다

> **왜 16줄인가.** 사람이 치는 명령이 16줄이나 밀리는 상황은 `[loop]`가 이미 막혔다는 뜻이고,
> 그때 큐를 키워도 처리되지 않는다.

가장 오래된 것을 버리는 쪽이 지표 품질로는 낫지만, 그러려면 생산자가 소비자의 tail을
움직여야 해서 락이 필요하다. **지표 하나 더 살리자고 `[loop]`에 락을 들이지 않는다.**
뮤텍스로도 임계 구역이 짧아 실제로는 거의 안 막히겠지만, "거의"는 보장이 아니다. 소비자가
락을 쥔 채 스케줄에서 밀리면 `[loop]`가 그만큼 멈춘다.

Phase 5의 "서버 중단 10분" 시험(NFR-3)은 이 격리 없이는 통과할 수 없다. 4스레드 안에서는
응답하지 않는 **텔레메트리 서비스**로의 TCP 업로드가 타이머 스레드를 수십 초 막고, 그 사이
keepalive가 나가지 않아 NAT 매핑이 만료된다. 원격 서비스 장애가 터널을 끊는다.

**서비스를 둘로 나눠도 이 격리는 여전히 필요하다.** 분리는 텔레메트리 장애가 제어 평면을 막지
않게 할 뿐이고, 클라이언트 안에서 업로드가 `[loop]`를 막는 문제는 스레드 격리만이 막는다.

#### 3.2.7 종료

- `[telemetry]`는 `WaitForSingleObject(shutdown_ev, 1000)`으로 1초마다 깨어 큐를 비운다.
  push마다 이벤트를 신호하지 않는다. 종료 이벤트는 수동 리셋이라 신호되는 즉시 이 대기도 함께
  깨운다.
- `[telemetry]`는 업로드 소켓에 `SO_SNDTIMEO` / `SO_RCVTIMEO` 1.5초를 건다. 업로드와 업로드
  사이마다 종료 이벤트를 확인한다.

**1.5초가 업로드 한 번의 총 시간을 2초 아래로 만들지는 못한다.** 두 타임아웃은 연산별이라
송신과 수신이 각각 걸리면 합이 3초까지 간다. 그래서 join 상한을 지키는 것은 이 값이 아니라
아래의 `_exit`다. 1.5초를 쓰는 이유는 **흔한 경우를 빨리 끝내는 것**이고, 더 키우면 그
효과마저 사라진다.

`SetConsoleCtrlHandler`가 종료 이벤트를 신호한다. **핸들러는 `[loop]` 소유 상태를 절대
건드리지 않는다.** 다른 스레드 컨텍스트에서 실행되기 때문이다.

**제어 신호 종류에 따라 반환 시점이 다르다.**

| 신호 | 핸들러 |
|------|--------|
| `CTRL_C_EVENT`, `CTRL_BREAK_EVENT` | 이벤트를 신호하고 즉시 `TRUE`를 반환하면 된다 |
| `CTRL_CLOSE_EVENT`, `CTRL_LOGOFF_EVENT`, `CTRL_SHUTDOWN_EVENT` | 종료 이벤트를 신호한 뒤 정리 완료 이벤트를 OS 유예 시간 안쪽(3초)까지 기다렸다가 반환한다 |

뒤의 세 신호는 **핸들러가 반환하는 순간 Windows가 프로세스를 종료한다.** 신호만 하고
돌아오면 `CLOSE` 송신도 어댑터 정리도 실행되지 않는다. 콘솔 창을 닫는 것은 이 프로그램의
정상적인 종료 방법이므로 예외 처리가 아니라 주 경로다.

`[loop]`의 `shutdown()`은 아래 순서로 돈다. `[control]`은 Phase 3 이후에만 있으므로 그 전에는
(8)이 `[telemetry]`만이다.

| # | 하는 일 |
|---|---------|
| 1 | 종료 이벤트 신호 (아직 안 됐으면) |
| 2 | [`protocol.md`](protocol.md) 5.6 `CLOSE` 가 정한 대상 세션마다 `CLOSE` 1회 송신. 대상과 reason 값은 그 절이 정한다. 여기에 조건을 다시 적지 않는다 |
| 3 | **아직 끝나지 않은 세션이 `CONNECTED`에 도달했었다면 로컬 기록 파일에 종료 줄을 쓴다** (9장. 이미 종료 상태라 그 줄을 쓴 세션은 다시 쓰지 않는다) |
| 4 | **카운터 전량을 `counter` 이벤트로 낸다** (9장) |
| 5 | 어댑터 세션 종료 |
| 6 | 어댑터/주소/라우트 정리 |
| 7 | **정리 완료 이벤트 신호** |
| 8 | `[telemetry]`와 `[control]` join |

- **종료 상태에 도달하면 프로세스도 끝난다.** 세션이 `FAILED`나 `CLOSED`가
  되면(`protocol.md` 9.6 실패 전이, 5.6 `CLOSE`) `[loop]`는 그 세션의 기록 줄과 카운터를 남긴
  뒤 스스로 `shutdown()`으로 간다. 최소 범위의 세션은 하나이므로 그 세션이 끝나면 프로세스가
  할 일이 없다. 사용자의 `quit`을 기다리지 않는다. **자동 재시도를 정하면 이 줄을 다시
  본다**(`protocol.md` 10.4 엔드포인트 학습의 미정 항목). 재시도가 생기면 `FAILED`가 곧 종료가
  아니게 된다.
- **큐에 종료 표식을 넣지 않는다.** 큐는 가득 차면 새 항목을 버리므로 하필 그때 표식이
  버려지면 `[telemetry]`가 종료를 영영 못 본다. 종료는 큐 밖의 이벤트로만 전달한다.
- **정리가 join보다 먼저다.** 이 순서 덕분에 콘솔 핸들러는 텔레메트리를 기다리지 않고 반환할
  수 있고, OS가 그 시점에 프로세스를 죽여도 잃는 것은 지표 몇 건뿐이다.
- join 상한은 두 스레드 합쳐 2초다. 넘기면 남은 레코드와 진행 중인 제어 요청을 포기하고 join을
  건너뛴 채 `_exit`로 즉시 끝낸다. 정리가 (7)에서 이미 끝났으므로 안전하다. **상한을 실제로
  강제하는 것은 소켓 타임아웃이 아니라 이 `_exit`다.** 소켓 타임아웃은 흔한 경우를 깔끔하게
  끝내줄 뿐이고, 어떤 경우에도 종료가 텔레메트리 서비스 가용성에 인질 잡히지 않게 하는 것은
  `_exit`다.
- **`[console]`은 join하지 않는다.** 이 스레드는 표준 입력 읽기에서 블록한다. **이 설계는 그
  읽기를 밖에서 취소하는 수단을 쓰지 않는다.** 그래서 join하면 사용자가 한 줄을 더 입력할
  때까지 종료가 멈춘다. 종료 이벤트를 신호한 뒤 그대로 두고 프로세스 종료에 맡긴다.
  `[console]`이 소유한 것은 명령 큐뿐이고 정리할 외부 자원이 없으므로 잃는 것이 없다.
- 어댑터, 가상 IP 주소, 라우트의 정리와 비정상 종료 후 잔존물 처리는
  [`windows-prereq.md`](windows-prereq.md) 3절에 있다.

#### 3.2.8 `[control]` 스레드

제어 평면 TCP 호출과 DNS 해석은 `[loop]`에서 하지 않는다. `getaddrinfo`도 동기 호출이라 같은
자리다.

> **왜.** 제어 서버가 죽어 있으면 TCP `connect`가 SYN 재시도로 수십 초를 소모한다. Phase 4
> 검증이 일부러 만드는 상황이다. 그 시간이 `[loop]`에서 흐르면 `HELLO` 재전송(200ms)과
> keepalive(15s)와 종료 이벤트 응답이 전부 멈춘다. 3.2.6 텔레메트리 격리가 4스레드 안을
> 폐기한 사유가 다른 문으로 돌아오는 것이고 [`spec.md`](spec.md) NFR-3 위반이다.

**논블로킹 소켓을 `[loop]`의 대기 집합에 넣는 안을 쓰지 않는다.** TCP 연결, HTTP 파싱, DNS를
전부 상태 머신으로 다시 써야 하고, `getaddrinfo`는 논블로킹 형태가 없다. 스레드 하나가 싸다.

| 항목 | 값 |
|------|-----|
| 큐 | `[loop]` -> `[control]` 요청 큐, `[control]` -> `[loop]` 응답 큐. 각각 SPSC 링 **8 항목**. 3.2.6 텔레메트리 격리의 링과 같은 구현이고 락이 없다 |
| 가득 찼을 때 | 요청 큐가 차면 `[loop]`는 그 요청을 버리고 `control_queue_dropped`를 올린 뒤 **그 시도를 `CONTROL_PLANE_EXCHANGE_FAILED`로 끝낸다.** 요청은 한 번에 하나만 미결이므로(아래) 정상 경로에서는 차지 않는다. 차면 `[control]`이 멈춘 것이다 |
| 미결 요청 | **한 번에 하나.** `[loop]`는 응답을 받기 전에 다음 요청을 넣지 않는다. `get_peers` 폴링은 "응답 수신 후 500ms" 이다. 폴링 타이머가 만료했는데 앞 요청이 미결이면 그 바퀴는 건너뛴다 |
| 깨우기 | `[control]`은 `WaitForMultipleObjects`로 종료 이벤트와 요청 이벤트(자동 리셋, `[loop]`가 신호)를 기다린다. `[loop]`는 응답 이벤트(3.2.2 대기의 순위 4)로 깨어난다 |
| 소켓 | 요청마다 새 TCP 소켓. 논블로킹 `connect` + `select`로 연결 시간 제한, `SO_SNDTIMEO`/`SO_RCVTIMEO`로 송수신 시간 제한. 값은 [`control_plane.md`](control_plane.md) 8.2 시간 제한 |
| DNS | 기동 시 한 번. 결과 IPv4 주소 하나를 `[control]`이 들고 이후 모든 요청에 쓴다. 이것이 `[control]`이 갖는 유일한 상태다 |
| 종료 | 종료 이벤트를 보면 진행 중인 요청의 응답을 기다리지 않고 소켓을 닫고 반환한다. 소켓 시간 제한 때문에 최대 한 요청 상한(`control_plane.md` 8.2 시간 제한)만큼 늦을 수 있고, 그것을 3.2.7 종료의 join 상한 2초와 `_exit`가 가둔다 |

**`[control]`은 세션 상태를 읽지도 쓰지도 않는다.** 요청 큐에서 꺼낸 것을 보내고 받은 것을
응답 큐에 넣는다. 응답을 세션 상태에 반영하는 것은 `drain_control`이고 그것은 `[loop]`다.
응답 해석(JSON 파싱, 오류 분류)은 `[control]`이 해도 된다. 그 결과는 값이고 공유 상태가
아니다.

**`[telemetry]`와 합치지 않는다.** 텔레메트리 업로드는 유실을 허용하고 실패를 보고하지
않는다. 제어 요청은 응답이 필요하고 실패가 곧 `CONTROL_PLANE_EXCHANGE_FAILED`다. 한
스레드에서 두 성질을 섞으면 텔레메트리 서비스 장애가 제어 요청을 지연시켜 `get_peers`
마감(60s)을 잡아먹는다. 두 서비스를 분리한 이유(3.4 텔레메트리 서비스)를 클라이언트 안에서
다시 무너뜨리는 셈이다.

---

### 3.3 제어 평면 (Python)

**제어 평면의 단일 출처는 [`control_plane.md`](control_plane.md)다.** 연산, 인코딩, 오류,
식별자, 방과 피어의 상태 전이, DynamoDB 테이블 설계, 저장 계약, 서버 모듈 구조, 클라이언트 쪽
호출 계약이 전부 그 문서에 있다. 이 절은 아키텍처 수준의 사실만 적는다.

- Python 표준 라이브러리(`asyncio`, `json`)와 AWS SDK(`boto3`)를 쓴다. 웹 프레임워크는 쓰지
  않는다. **`boto3` 는 표준 라이브러리가 아니다.** [`spec.md`](spec.md) NFR-5 에 따라
  승인받았다
- 상태는 Amazon DynamoDB 에 두고 서버는 방·피어 상태를 메모리에 들지 않는다. 저장 계약 다섯은
  `control_plane.md` 6.1 저장 계약이 갖는다
  - **왜 이렇게 정했고 무엇을 버렸는지는** [ADR 0004](decisions/0004-상태-저장소-dynamodb.md)가
    갖는다
- 그래서 "재시작 복원" 절차가 없다. `spec.md` NFR-3 이 요구하는 재시작 후 방 상태 유지는 복원
  절차가 아니라 저장 계약으로 성립한다
- 게임 트래픽도, 지표도, UDP 도 다루지 않는다. 하지 않는 것의 목록은 `control_plane.md` 1.1
  하는 것과 하지 않는 것

### 3.4 텔레메트리 서비스 (Python)

**제어 평면과 분리된 별개 서비스다.** 제어 평면의 모듈이 아니다.

| 모듈 | 책임 |
|------|------|
| `server.py` | HTTP/JSON 수집 엔드포인트 |
| `ingest.py` | 연결 결과와 성능 지표 수신, 검증, DynamoDB 저장 |

분리의 계약은 넷이다.

| 계약 | 내용 |
|------|------|
| 분리 1 | 제어 평면은 텔레메트리 서비스를 호출하지 않는다 |
| 분리 2 | 텔레메트리 서비스는 제어 평면을 호출하지 않는다. 받은 식별자의 유효성을 제어 평면에 묻지 않는다 |
| 분리 3 | 두 서비스의 접점은 식별자 이름 공간뿐이다. `room_id` 와 `peer_id` 는 제어 평면이 발급하고 텔레메트리는 그것을 불투명한 값으로 기록한다. 대조는 분석 시점에 한다 |
| 분리 4 | 텔레메트리 서비스 프로세스가 멈춰도 제어 평면의 연산은 **호출 경로에서** 막히지 않는다. 계약의 범위는 서비스 간 호출까지다. 같은 인스턴스의 공유 자원 고갈은 이 계약이 덮지 않는다 |

**접점이 식별자뿐이라서 텔레메트리가 받은 값은 신뢰할 수 없다.** 제어 평면이 발급한 적 없는
`room_id` 도 그대로 저장된다. 이것을 수집 시점에 막으려면 텔레메트리가 제어 평면에 물어봐야
하고 그러면 분리 2가 깨진다. 그래서 막지 않는다. 대신 분석 시점에 제어 평면 쪽 기록과 대조해
걸러낸다. 이 한계와 대조 방법은 [`roadmap.md`](roadmap.md) Phase 9 가 갖는다.

**같은 EC2 인스턴스에 두는 것은 배포 편의지 결합이 아니다.** 다만 한 대에 있는 동안은 CPU와
디스크를 공유한다.

**분리 4는 그 위험을 덮지 않는다.** 텔레메트리가 디스크나 CPU를 고갈시키면 제어 평면도 같이
멈추고, 그것은 이 계약을 지켜도 일어난다. 설계상의 의존이 없다는 것과 자원이 격리됐다는 것은
다른 말이다.

**자원 격리는 아직 없다.** 무엇으로 나눌지는 `roadmap.md` Phase 9 의 착수 전 항목이 갖는다.

---

### 3.5 기동 입력

클라이언트가 기동 시 받는 값과 전달 방식이다. **Phase 1~5 는 CLI 인자만 쓴다.** 설정 파일은 두지 않는다. 값이 적고, 시험마다 바뀌는 값을 파일에 두면 어느 파일로 돌렸는지가 기록에 남지 않는다. 파일이 필요해지는 시점은 [`roadmap.md`](roadmap.md) 가 정한다.

| 입력 | 인자 | 필수 | 값 |
|------|------|:---:|-----|
| 역할 | 첫 위치 인자 `host` 또는 `player` | Phase 3 이후 예. Phase 1~2 는 받아서 보관만 하고 없어도 기동한다 | `host` 는 `create_room`, `player` 는 `join_room` 을 부른다 |
| 제어 서버 주소 | `--server <이름 또는 IPv4>[:<포트>]` | Phase 3 이후 예. Phase 1~2 는 받아서 보관만 하고 없어도 기동한다 | DNS 이름 또는 IPv4 리터럴. 포트를 생략하면 [`control_plane.md`](control_plane.md) 2.6 상수의 `CONTROL_PORT`. 문서에 실제 주소를 박지 않는다 |
| 방 코드 | `--room <6자>` | `player` 는 항상. `host` 는 `--rejoin` 을 같이 줄 때. Phase 3 이후 | `control_plane.md` 2.1 `room_id` 의 형식. 소문자를 쳐도 된다. 서버가 대문자로 정규화한다 |
| 재참가 증명 | `--rejoin <peer_id>:<peer_token>` | 아니오 | 있으면 `join_room` 을 재참가 형식으로 부른다 (`control_plane.md` 4.3 `join_room`). `host` 역할에도 쓸 수 있다. 재시작한 호스트도 재참가다 |
| STUN 서버 | `--stun <이름 또는 IPv4>:<포트>` 반복 | 아니오 | 주면 아래 기본 목록을 **통째로 대체**한다. 목록이 한 개일 때의 거동은 표 아래에 있다 |

**`--stun` 목록이 한 개면 기동 시 `WARN` 한 줄을 남긴다.** 그 목록으로는 서로 다른 두 서버의
응답을 얻을 수 없어 아래 "서버 선택" 대로 `STUN_DISCOVERY_FAILED` 로 끝난다.

> **왜.** 경고를 기동 시점에 내는 이유는 5초를 기다린 뒤 실패를 보는 것보다 입력이 틀렸다는
> 것을 먼저 알리는 편이 낫기 때문이다.

**STUN 기본 목록.** 출처는 [`tools/nat-probe/natprobe.py`](../../tools/nat-probe/natprobe.py) 의 `DEFAULT_STUN_SERVERS` 다. 실측 22건이 그 목록으로 돌았다. **두 곳의 값이 같아야 한다.** 한쪽을 바꾸면 다른 쪽도 같이 바꾼다.

```text
stun.l.google.com:19302
stun1.l.google.com:19302
stun.cloudflare.com:3478
stun.nextcloud.com:3478
```

**서버 선택.** 목록의 앞 두 서버에 같은 소켓으로 **동시에** 질의한다. 트랜잭션 ID 가 응답을
가른다([`protocol.md`](protocol.md) 13장 STUN 사용 범위). STUN 마감은 서버별이다(`protocol.md`
11장 타이머). 한 서버가 자기 마감에 걸리면 목록의 다음 서버로 바꿔 다시 질의하고, 그 서버의
재시도 일정과 마감은 새로 시작한다. 단계 전체의 상한은 그 문서가 계산한다.

**목록을 다 써도 서로 다른 두 서버의 응답을 얻지 못하면 `STUN_DISCOVERY_FAILED` 다.** 응답이
하나뿐이어도 실패다.

> **왜.** M-2 와 "지원 네트워크 조건" 판정에 둘이 필요하고, 하나로 진행하면 목적지 의존 매핑을
> 구분하지 못한 채 펀치에 들어가 그 실패가 NAT 실패로 기록된다.

**STUN 서버 이름은 기동 시 한 번 해석한다.** 기본 목록이 전부 DNS 이름이므로 해석이 필요하고,
그 호출(`getaddrinfo`)은 동기라 `[loop]` 안에 두지 않는다.

- **`[loop]` 시작 전에 목록 전체를 해석한다.** Phase 1~2 에는 `[control]` 스레드가 없으므로
  (3.2.1 스레드) 그 자리를 빌릴 수도 없다
- 결과가 여럿이면 첫 IPv4 주소 하나를 쓴다. `control_plane.md` 3.2 주소와 같은 규칙이다.
  `AAAA` 는 쓰지 않는다
- **해석에 실패한 항목은 목록에서 빼고 `WARN` 한 줄을 남긴다.** 남은 목록이 두 개 미만이면 위
  규칙대로 `STUN_DISCOVERY_FAILED` 다

제어 서버 주소와 달리 여기서는 해석 실패가 기동 실패가 아니다. 목록이 여럿이라 하나가 죽어도
진행할 수 있다.

**재참가 증명의 보관.** `join_room` 과 `create_room` 응답의 `peer_id` 와 `peer_token` 을
클라이언트가 **표준 출력**에 한 줄 낸다. 사람이 그것을 복사해 `--rejoin` 으로 넘긴다. 파일에
자동 보관하지 않는다. 자동 재시도가 미정이라(`protocol.md` 10.4 엔드포인트 학습) 그것을 정할
때 보관 방식도 같이 정한다.

**방 코드도 표준 출력이다.** 호스트는 `create_room` 응답의 `room_id` 를 상대에게 전해야 한다.
표준 오류의 로그(9장)에는 싣지 않는다.

> **왜.** 로그 파일이 곧 방 코드 목록이 되면 로그 열람이 방 탈취다. 표준 출력은 사람이 보는
> 화면이고 리다이렉트하지 않는 것이 기본이다.

```text
ROOM <room_id>
REJOIN <peer_id>:<peer_token>
```

두 줄의 첫 낱말은 고정이다. 시험이 이것으로 값을 읽는다.

**Phase 1~2 의 시험용 인자 둘.** 위 표는 제품 경로의 입력이다. Phase 1 은 제어 평면도 STUN 도
없이 "알려진 두 엔드포인트" 로 송수신을 확인하므로(`roadmap.md` Phase 1) 상대 주소를 받을
자리가 필요하다.

| 입력 | 인자 | 값 |
|------|------|-----|
| 상대 엔드포인트 | `--peer <IPv4>:<포트>` | Phase 1~2 시험 전용. 없으면 송신하지 않고 수신만 한다. 이 인자로 보내는 것은 터널 헤더가 없는 시험 바이트열이고 Phase 4 이후의 `DATA` 와 다르다 |
| 원시 송신 | 콘솔 명령 `raw <바이트 수>` | `--peer` 로 받은 주소에 그 길이의 데이터그램을 1개 보낸다. 내용은 `0x00` 부터 1씩 증가하는 바이트열이라 수신 측이 계산으로 대조할 수 있다. 길이는 1 이상 `MAX_DATAGRAM`(1472) 이하. 범위 밖이면 보내지 않고 `WARN` |
| 카운터 출력 | 콘솔 명령 `counters` | 카운터 전량을 즉시 낸다. 9장이 정한 세 시점 중 하나다 |
| 종료 | 콘솔 명령 `quit` | 종료 이벤트를 신호한다. 3.2.3 루프 한 바퀴가 그 경로를 정한다 |

**수신 측은 `rx.raw` 로그 한 줄을 낸다.** 필드는 `from`(출발지 `IPv4:port`), `len`,
`sha256`(수신 바이트열의 16진 해시 앞 16자)이다. 바이트열 자체를 로그에 싣지 않는다. **송신
측도 같은 줄을 낸다.** 두 줄의 `len` 과 `sha256` 이 같으면 Phase 1 의 "바이트 단위로 일치" 가
성립한다.

**송신 측의 `from` 은 자신의 로컬 엔드포인트다.** 그 데이터그램의 출발지가 자신이기
때문이다. bind 가 `INADDR_ANY` 이므로 주소 부분은 `0.0.0.0` 이다(`protocol.md` 6장). 판정이
보는 것은 `len` 과 `sha256` 둘이고 `from` 은 어느 쪽 줄인지를 사람이 가르는 데 쓴다.

> **왜.** 눈으로 바이트를 비교하는 절차를 두지 않는 이유는 그것이 사람마다 다른 판정이 되기
> 때문이다.

**둘 다 Phase 6 에서 사라진다.** `[console]` 스캐폴딩과 같은 수명이다(3.2.1).

**기동 입력이 아닌 것.** 로컬 기록 파일 경로는 9장의 계약이 정해지면 그때 같이 정한다. 텔레메트리 서비스 주소는 Phase 9 다. 둘 다 여기 표에 없다는 것이 현재 상태다.

**인자 오류의 거동.** 알 수 없는 인자, 필수 인자 누락, 형식 위반은 전부 **`ERROR` 한 줄을
남기고 종료 코드 2로 기동 실패**다. 추측해서 고쳐 받지 않는다.

- 같은 인자를 두 번 주면 `--stun` 은 목록에 쌓이고 나머지는 마지막 값이 이긴다
- **준 인자는 그 구간에서 쓰지 않아도 검사한다.** 위 표의 "보관만 한다" 는 값을 쓰지 않는다는
  뜻이지 검사하지 않는다는 뜻이 아니다

> **왜.** 검사를 미루면 Phase 1 에서 통과한 입력이 Phase 3 에서야 거부되고, 그 사이 시험이
> 어느 형식으로 돌았는지 기록에서 읽을 수 없게 된다.

**누락 판정은 위 표의 "필수" 열이 정한다.** 그 열이 요구하는 구간에서 없는 것이 누락이다.
역할은 Phase 1~2 에 없어도 기동한다. 다만 값이 `host` 도 `player` 도 아니면 어느 구간에서나
형식 위반이다.

**형식의 출처는 값마다 다르다.** 규칙을 여기에 옮겨 적지 않는다.

| 값 | 출처 |
|----|------|
| `--room` | `control_plane.md` 2.1 `room_id` 의 정규화와 검사 |
| `--rejoin` 의 `peer_id` | `control_plane.md` 2.2 `peer_id`. 10진수로 적는다 |
| `--rejoin` 의 `peer_token` | `control_plane.md` 2.3 `peer_token` |
| `--server` 의 포트 | 생략하면 `control_plane.md` 2.6 상수의 `CONTROL_PORT` |
| 모든 포트 | 1~65535 |
| `--peer` 의 주소 | IPv4 리터럴만 받는다. 이름을 받지 않는다 |
| `--stun` 의 주소 | 이름 또는 IPv4 리터럴. 포트는 생략할 수 없다 |

**반례 목록은 시험이 갖는다.** IPv4 리터럴, 포트 범위, `--room`, `--rejoin` 의 케이스 표는
`tests/` 에 있다. 문서에 두면 같은 표를 두 곳에서 맞춰야 하고, 반례는 구현이 자라면서 는다.

---

## 4. 데이터 평면 경로

게임 패킷이 상대 머신에 도달하기까지의 전체 경로다.

```text
Minecraft (TCP, 10.100.0.2 -> 10.100.0.1:25565)
   |
   v  Windows 라우팅 테이블이 10.100.0.0/24 를 가상 어댑터로 보냄
Wintun Adapter (송신측)
   |
   v  wintun_adapter 가 원본 IP 패킷을 읽음
Router: 목적지 가상 IP -> 피어 세션 결정
   |
   v  tunnel 이 DATA 헤더를 붙임
[TunnelHeader][원본 IP 패킷]
   |
   v  udp_socket 이 직접 P2P 경로로 송신
Internet (UDP, 공인 엔드포인트 -> 공인 엔드포인트)
   |
   v  상대 udp_socket 수신
tunnel: 헤더 검증 및 제거
   |
   v  wintun_adapter 가 원본 IP 패킷을 주입
Wintun Adapter (수신측)
   |
   v
Minecraft Server
```

Minecraft Java Edition은 게임플레이에 TCP를 쓰지만, 터널 자체는 UDP를 쓴다. 원본 TCP 패킷이 페이로드로 실려 가기 때문에 문제되지 않는다. TCP 재전송과 혼잡 제어는 게임의 TCP 스택이 그대로 담당한다.

### MTU 처리

터널 헤더가 붙으므로 페이로드 가용 크기가 줄어든다.

```text
1500 (경로 MTU 가정)
 - 20 (외부 IPv4 헤더)
 -  8 (외부 UDP 헤더)
 - 20 (TunnelHeader)
= 1452 바이트 (내부 IP 패킷 최대치)
```

가상 어댑터 MTU 기본값은 [`protocol.md`](protocol.md) 12장이 소유한다. 위 1452바이트보다 여유를 둔 값이며, 여유폭의 근거와 DF 비트 취급도 그 장에 있다. **여기에 숫자를 복사하지 않는다.** 그 값은 실측으로 확정하는 대상이라(`roadmap.md` Phase 7) 복사해 두면 확정 시점에 이 문서가 낡는다.

다만 위 계산은 **외부 경로 MTU가 1500이라는 가정**에 의존한다. PPPoE 회선이나 다른 터널을 경유하는 경로는 이보다 작고, 그 경우 외부 UDP 패킷이 조각화된다. 가정이 깨지는 경로의 실측과 한계 기록도 같은 장이 정한다.

---

## 5. 터널 프로토콜

### 5.1 헤더

```text
 오프셋  크기  필드              설명
   0      4   magic             0x53414E47 ("SANG")
   4      1   version           0x01
   5      1   type              PacketType
   6      2   payload_length    헤더 뒤 바이트 수
   8      4   peer_id           송신 피어 식별자
  12      4   session_epoch     송신 피어의 이번 세션 시도 식별자
  16      4   sequence          방향별 단조 증가 카운터
  = 20 바이트, 네트워크 바이트 오더
```

`session_epoch`는 **세션 시도 단위**이고 프로세스 단위가 아니다. 이 필드가 없으면 재시작이나
재시도 후 이전 시도의 지연 패킷이 새 세션에 수용된다.

수신 측은 첫 유효 패킷에서 상대 epoch를 고정하고, 이후 다른 epoch의 `HELLO`는 상대가 새 시도를
시작했다는 뜻이므로 폐기가 아니라 재협상으로 처리하고, 나머지 타입은 폐기한다. 추첨 방법, 폐기
목록, 재협상 절차는 [`protocol.md`](protocol.md) 4.3 `session_epoch` 와 9.3 epoch 규칙, 9.5
재협상이 갖는다.

4바이트 필드가 모두 4의 배수 오프셋에 오도록 배치해 MSVC 기본 정렬에서도 패딩이 생기지 않는다. 그래도 구조체를 그대로 `memcpy` 하지 않는다. 바이트 오더 변환이 필요하고, 정렬이 우연히 맞는 것에 의존하면 조용히 깨진다. **필드 단위로 직렬화**하고 길이는 상수로 고정한다.

> **상세는 `protocol.md` 에 있다.** 상수 값, 페이로드 레이아웃, 수신 검증 파이프라인, 후보
> 지명, 전이표 전체, 확정 타이머 값은 그 문서가 유일한 출처다. 이 절은 요약이며 충돌하면
> `protocol.md`가 우선한다.

### 5.2 패킷 타입

| 타입 | 방향 | 페이로드 | 용도 |
|------|------|----------|------|
| `HELLO` `0x01` | 양방향 | nonce(16) + 가상 IP(4) | 홀펀칭 시도 및 핸드셰이크 개시 |
| `HELLO_ACK` `0x02` | 양방향 | echo nonce(16) + 가상 IP(4) | 상대가 내 패킷을 실제로 받았음을 증명 |
| `KEEPALIVE` `0x03` | 양방향 | 없음 | NAT 매핑 유지 (주기 15초) |
| `DATA` `0x04` | 양방향 | 내부 IPv4 패킷 | 게임 트래픽 전달 |
| `PING` `0x05` | 양방향 | ping_id(8) | RTT 측정. 타임스탬프는 와이어에 싣지 않는다 |
| `PONG` `0x06` | 양방향 | ping_id 에코(8) | RTT 측정 응답 |
| `CLOSE` `0x07` | 양방향 | reason(1) | 정상 종료 통지. 없으면 상대가 50초 유휴 타임아웃까지 죽은 세션을 붙든다 |

### 5.3 세션 상태 머신

```text
IDLE
  | 제어 평면에서 피어 후보 수신
  v
PUNCHING  --- 펀치 마감 --->  FAILED(HOLE_PUNCH_TIMEOUT)
  | got_ack 또는 sent_ack 중 하나가 섬 (어느 쪽인지는 정해져 있지 않다)
  v
HANDSHAKING --- 펀치 마감 --->  FAILED(PEER_HANDSHAKE_FAILED)
  | 남은 하나가 섬
  v
CONNECTED
  | 유휴 타임아웃 (상대 패킷 무수신)
  v
FAILED(TUNNEL_DROPPED)
```

UDP `sendto`의 성공은 전달을 보장하지 않는다. 따라서 두 전이 모두 **수신한 패킷**으로만
판정한다.

- `HANDSHAKING -> CONNECTED`: **두 개의 독립적인 플래그가 모두 서야 한다.** `got_ack`(우리가
  보낸 nonce를 echo한 `HELLO_ACK` 수신, 우리 -> 상대 경로 확인)와 `sent_ack`(상대의 유효한
  `HELLO`에 `HELLO_ACK` 송신, 상대 -> 우리 경로 확인)이다.
  - 어느 쪽이 먼저 서는지는 정해져 있지 않다. 순서를 가정하면 한쪽만 뚫린 경로를 연결로
    오판하거나 정상 순서에서 양쪽이 타임아웃한다
- `CONNECTED -> FAILED`: keepalive 송신 실패가 아니라, 50초 동안 상대로부터 유효 패킷을 하나도
  받지 못한 것으로 판정한다. keepalive는 15초 간격이고 `CONNECTED` 진입 즉시 1회 보내므로 3회
  손실을 견딘다.
- 펀치 마감에 걸린 두 전이는 같은 마감을 쓰고 **그 시점에 선 플래그 개수로만** 코드가 갈린다.
  판정 근거는 8장 실패 진단에 있다.

**그림에 없는 되돌아가는 전이가 하나 있다.** 상대가 새 시도를 시작하면(다른 `session_epoch`의
`HELLO`) 세션은 `CONNECTED`에서도 왕복 증거를 지우고 `CONNECTED` 이전으로 돌아간다. 절차와
그때의 상태 판정은 [`protocol.md`](protocol.md) 9.5 재협상이 갖는다. 이 문서만 읽고 전이가
단방향이라고 구현하면 그 경로가 빠진다.

위 그림은 정상 경로의 뼈대다. **활성 상태 셋**(`PUNCHING`, `HANDSHAKING`, `CONNECTED`)과 모든
수신 패킷의 전이는 `protocol.md` 9.4절 전이표에 있고, `IDLE`과 종료 상태(`FAILED`, `CLOSED`)의
수신 처리는 9.4.1절이 따로 맡는다. 특히 `PUNCHING`에서 `HELLO_ACK`이 먼저 도착하는 것과
`CONNECTED`에서 `HELLO`에 다시 응답하는 것은 정상 동작이며, 이를 빠뜨리면 양쪽이 타임아웃한다.

---

## 6. 제어 평면 인터페이스

### 6.1 연산

연산은 넷이다. `create_room`, `join_room`, `register_candidate`, `get_peers`. 입력, 출력, 오류,
인코딩은 [`control_plane.md`](control_plane.md) 4장 연산이 갖는다. **`register_peer` 는
없다.** 재참가는 `join_room` 의 한 형식이다.

**`report_connection` 과 `report_telemetry` 는 여기에 없다.** 둘 다 텔레메트리 서비스의
연산이고 6.3 텔레메트리 서비스 인터페이스가 갖는다. 연결 결과는 9장 지표 표의 첫 줄(`연결
성공/실패 + 실패 단계`)이므로 지표와 같은 곳으로 간다.

### 6.2 연결 수립 시퀀스

```text
Host                 AWS (조율 / 텔레메트리)            Player
 |                         |                          |
 |--- create_room -------->|                          |
 |<-- room_id, 10.100.0.1--|                          |
 |                         |<------- join_room -------|
 |                         |-- room_id, 10.100.0.2 -->|
 |                         |                          |
 |-- STUN Binding Req ->(공개 STUN 서버)<- STUN Binding Req --|
 |<- XOR-MAPPED-ADDRESS -                - XOR-MAPPED-ADDRESS ->|
 |                         |                          |
 |-- register_candidate -->|<-- register_candidate ---|
 |                         |                          |
 |--- get_peers ---------->|<-------- get_peers ------|
 |<-- 상대 엔드포인트 -------|--- 상대 엔드포인트 ------->|
 |                         |                          |
 |=========== UDP HELLO 동시 송신 (홀펀칭) =============|
 |=========== Direct P2P Tunnel 수립 =================|
 |                         |                          |
 |--- report_connection -->|<--- report_connection ---|
```

**마지막 줄의 `report_connection` 만 목적지가 다르다.** 조율 서버가 아니라 텔레메트리 서비스로 간다. 그림에서 AWS를 한 칸으로 그린 것은 두 서비스가 같은 인스턴스에 있기 때문이고, 같은 서비스라는 뜻이 아니다.

### 6.3 텔레메트리 서비스 인터페이스

| 연산 | 입력 | 출력 |
|------|------|------|
| `report_connection` | room_id, peer_id, 결과, 실패 단계, 수립 시간 | 확인 |
| `report_telemetry` | room_id, peer_id, RTT/손실/지터/전송량 | 확인 |

**두 연산은 실패해도 클라이언트가 재시도하지 않는다.** 다만 둘은 주기가 다르다.

- `report_telemetry` 는 주기 업로드라 실패하면 다음 주기로 넘어간다
- **`report_connection` 은 연결 시도 한 건마다 한 번뿐이라 넘어갈 다음 주기가 없다.** 그 시도의
  업로드는 그것으로 끝이고, 같은 내용이 로컬 기록 파일에 이미 남아 있다(9장)

근거는 [`spec.md`](spec.md) NFR-3과 9장 로컬 기록 계약이다. 판정에 쓰이는 것은 로컬 기록이지 이
업로드가 아니다.

**실패를 삼키지는 않는다.** 업로드가 실패하면 `[telemetry]` 가 카운터
`telemetry_upload_failed` 를 올리고 로그에 한 줄 남긴다. 큐가 가득 차 버리는
것(`telemetry_queue_dropped`, 3.2.6 텔레메트리 격리)과 다른 카운터다.

> **왜.** 앞의 것은 원격이 받지 못한 것이고 뒤의 것은 보내기 전에 버린 것이라, 둘을 한
> 이름으로 세면 텔레메트리 서비스 장애와 클라이언트 과부하를 구분할 수 없다.

**이 실패는 로컬 기록 파일에 쓰지 않는다.** 9장 로컬 기록은 연결 시도 한 건마다 한 줄이라는
계약이고, 업로드 실패는 연결 시도가 아니다.

---

## 7. 가상 네트워크 설계

- 대역: `10.100.0.0/24`
- `10.100.0.1`: 방 생성자(호스트). 게임 서버가 여기서 돈다.
- `10.100.0.2` 이상: 참가자. 제어 평면이 순차 할당한다.
- 가상 IP는 방 단위로 유효하며, 방이 사라지면 회수한다. 방이 언제 사라지는지와 살아 있는 동안
  자리를 회수하지 않는 이유는 [`control_plane.md`](control_plane.md) 2.5 절(가상 IP 풀)과 5.1
  절(방)이 갖는다.

플레이어는 Minecraft 서버 주소란에 `10.100.0.1:25565`를 입력한다. 공인 IP나 포트를 알 필요가 없다.

Windows 라우팅은 가상 어댑터에 `10.100.0.0/24` 경로를 붙여 처리한다. 클라이언트가 어댑터 생성 시 Windows IP Helper API로 이 경로를 직접 설정하고, 종료 시 제거한다. Wintun은 주소나 라우트를 설정해 주지 않는다.

---

## 8. 실패 진단

연결 실패는 반드시 단계로 분류해 기록한다. "연결 안 됨"은 기록으로 받아들이지 않는다.

| 코드 | 단계 | 판정 근거 |
|------|------|-----------|
| `STUN_DISCOVERY_FAILED` | 공인 엔드포인트 발견 | STUN 응답 없음 또는 파싱 실패 |
| `CONTROL_PLANE_EXCHANGE_FAILED` | 후보 교환 | `get_peers` 폴링 마감 초과, 제어 평면 오류, `peer_id` 충돌. 조건 목록은 [`protocol.md`](protocol.md) 9.6 실패 전이가 갖는다 |
| `HOLE_PUNCH_TIMEOUT` | 홀펀칭 | 펀치 마감까지 `got_ack`와 `sent_ack`가 **둘 다 미성립** |
| `PEER_HANDSHAKE_FAILED` | 핸드셰이크 | 펀치 마감까지 둘 중 **하나만 성립** |
| `TUNNEL_DROPPED` | 유지 | 수립 후 유휴 타임아웃 동안 상대 패킷 무수신 |

**뒤의 두 코드는 패킷 종류가 아니라 플래그 성립 개수로 가른다.** 두 플래그의 뜻과 어느 쪽이
먼저 서는지 정해져 있지 않다는 점은 5.3 세션 상태 머신에 있고, 판정 규칙의 출처는
`protocol.md` 9.6 실패 전이다.

> **왜.** "상대 `HELLO`를 받았는가"로 가르면 `HELLO_ACK`이 먼저 도착하는 정상 경로에서
> `got_ack`만 선 채 마감을 맞은 세션이 `HOLE_PUNCH_TIMEOUT`으로 기록된다. 그 세션은 우리 패킷이
> 상대에게 닿았음을 이미 증명했으므로 홀펀칭은 실패하지 않았다. 코드가 틀리면 Phase 9의 실패
> 단계 분포가 그만큼 틀린다.

각 실패에는 확보 가능한 NAT 환경 정보(로컬 대역, 공인 엔드포인트, STUN 서버별 매핑 차이)를 함께 남긴다. `STUN_DISCOVERY_FAILED`는 발견 자체가 실패한 경우라 공인 엔드포인트가 없다. 모든 필드를 요구하지 말고 존재하는 것만 기록한다.

**NAT 유형을 단정하지 않는다.** RFC 5389 Binding 결과와 포트 변화만으로는 NAT 유형이나 필터링
거동을 판별할 수 없다. Phase 9의 분석은 "NAT 유형"이 아니라 **관측된 매핑 거동**(목적지에 따라
공인 포트가 바뀌는가)과 연결 성공 여부로 분류한다. 유형 판별이 필요해지면 RFC 5780 지원
서버나 통제된 다중 목적지 시험이 추가로 필요하며, 이는 초기 범위 밖이다.

---

## 9. 텔레메트리와 기록

| 지표 | 수집 지점 | 용도 |
|------|-----------|------|
| 연결 성공/실패 + 실패 단계 | 세션 수립 종료 시 | 신뢰성 분석 |
| 연결 수립 시간 | STUN 시작 ~ `CONNECTED` | 사용자 체감 분석 |
| RTT | `PING`/`PONG` 주기 측정 | 지연 분석 |
| 패킷 손실률 | `sequence` 간격 누락 집계 | 품질 분석 |
| 지터 | 연속 RTT 편차 | 품질 분석 |
| keepalive 로컬 송신 오류 | `[loop]` 송신 경로. 카운터는 `tx_err_send` ([`protocol.md`](protocol.md) 6장) | 안정성 분석. 로컬 `sendto` 오류만 세며 전달 실패의 근거가 아니다. 그래서 단절 판정은 유휴 타임아웃으로 한다 |
| 유휴 타임아웃 발생 횟수 | 수신 경로 | 안정성 분석 |
| 직접 연결 유지 시간 | `CONNECTED` 지속 구간 | 안정성 분석 |
| 터널 처리량 | 송수신 바이트 카운터 | 성능 분석 |
| 세션 지속 시간 | 게임 세션 전체 | Minecraft 안정성 |

클라이언트는 로컬 링 버퍼에 먼저 쌓고 전용 스레드가 주기적으로 **텔레메트리 서비스**에 보낸다.
업로드는 데이터 평면과 완전히 분리되어 있어 텔레메트리 서비스 장애가 터널에 영향을 주지
않는다. 격리 구조는 3.2.6 텔레메트리 격리에 있다.

### 로컬 기록 파일

**업로드와 별개로 로컬 기록 파일이 있다.** [`spec.md`](spec.md) M-6 이 요구하는 것은 이것이고, 제어 평면 저장이 아니다. 계약은 넷이다.

| 항목 | 계약 |
|------|------|
| 내용 | 연결 시도 한 건마다 결과(성공 또는 `spec.md` FR-13 의 실패 코드)와 RTT. **RTT 는 측정하지 못한 시도에서는 비운다** |
| 시점 | 두 번이다. **수립이 끝나는 시점**에 한 줄(성공이든 실패든), 그리고 세션이 끝나는 시점에 한 줄을 더한다 |
| 방식 | 추가 전용(append-only). 이전 줄을 고치지 않는다 |
| 독립성 | 텔레메트리 큐와 업로드 경로에 의존하지 않는다. 제어 평면과 텔레메트리 서비스가 **둘 다** 없어도 기록이 남는다 |

**RTT 가 없는 시도가 있다.** `PING` 송신은 `CONNECTED` 진입에 시작하므로(11장) 그 전에는
보낸 `PING` 이 없고, 따라서 `pending_pings` 에 항목이 없다. 그 구간에 `PONG` 이 도착하면
[`protocol.md`](protocol.md) 5.5 `PING` / `PONG` 에 따라 조회에 실패해 `drop_unmatched_pong`
으로 버려진다.

**즉 `CONNECTED` 이전에는 RTT 표본이 만들어지지 않는다.** `protocol.md` 9.4 전이표가 그
구간에서 `PING` 에 응답하고 `PONG` 을 매칭 절차에 넣는 것과 모순되지 않는다. 그래서 그 전에
끝난 시도(`STUN_DISCOVERY_FAILED`, `CONTROL_PLANE_EXCHANGE_FAILED`, `HOLE_PUNCH_TIMEOUT`,
`PEER_HANDSHAKE_FAILED`)에는 값이 없다.

**없는 것을 0 으로 적지 않는다.** 빈 값으로 적고, 빈 값의 표기는 줄 형식과 같이 정한다.

> **왜.** 0ms 는 측정된 값처럼 보이고 Phase 9 의 집계를 오염시킨다.

`TUNNEL_DROPPED` 줄에는 단절 직전까지의 마지막 RTT 표본을 적고, **표본이 하나도 없으면 그
줄도 비운다.** `CONNECTED` 직후 첫 `PONG` 전에 끊기면 그런 경우다. 규칙은 하나다. 없으면
비운다. 예외를 두지 않는다.

**줄이 둘인 이유.** `TUNNEL_DROPPED`는 수립이 끝난 **뒤에** 나오는 코드다(8장). 시점을 수립 종료 하나로 두면 그 줄을 쓸 자리가 없고, 이미 쓴 줄을 고쳐 적으면 위의 추가 전용 계약이 깨진다.

**RTT 도 같은 이유로 두 줄이 필요하다.** `PING` 송신은 `CONNECTED` 진입에 시작하므로(11장)
**수립이 끝나는 순간에는 RTT 표본이 아직 없다.** 첫 줄의 RTT 는 그래서 대개 비어 있다. 두
번째 줄이 세션이 끝날 때 쓰이므로 거기에 값이 들어간다.

> **왜.** 수립 줄을 첫 표본까지 미루지 않는 이유는 그 사이에 프로세스가 죽으면 기록이 아예
> 남지 않기 때문이다.

- **수립 줄**: 모든 시도에 쓴다. 결과 코드(성공 또는 실패). RTT 는 그 시점에 표본이 있으면
  적고 없으면 비운다
- **종료 줄**: **`CONNECTED` 에 도달한 시도에만 쓴다.** 종료 코드와 마지막 RTT 표본, 없으면
  빈 값. 종료 코드는 `CLOSED` 또는 `spec.md` FR-13 의 실패 코드 하나다
  - `TUNNEL_DROPPED` 가 흔한 경우다. 재협상으로 `CONNECTED` 이전으로 돌아간 뒤 펀치 마감에
    걸리면 `HOLE_PUNCH_TIMEOUT` 이나 `PEER_HANDSHAKE_FAILED` 가 종료 줄에 온다
    (`protocol.md` 9.5 재협상, 9.6 실패 전이). 코드 집합을 둘로 못박으면 그 경우에 쓸 값이
    없다

**수립에 실패한 시도는 한 줄이다.** `STUN_DISCOVERY_FAILED`, `CONTROL_PLANE_EXCHANGE_FAILED`,
`HOLE_PUNCH_TIMEOUT`, `PEER_HANDSHAKE_FAILED` 는 `CONNECTED` 에 닿지 못한 것이므로 끝낼 세션이
없다. 수립 줄이 곧 그 시도의 마지막 줄이다. **줄 수로 도달 여부를 읽을 수 있다.** 한 줄이면
수립 전에 끝났고, 두 줄이면 연결됐다가 끝났다.

두 줄을 한 시도로 묶는 방법은 줄 형식과 같이 정한다.

**경로와 줄 형식은 아직 정하지 않았다.** 이 문서에서 정한다. 코드에서 임의로 정하지 않는다. **언제 정해야 하는지는 [`roadmap.md`](roadmap.md)가 갖는다.**

### 로그 출력

Phase 2~3의 검증 항목이 "`STUN_DISCOVERY_FAILED` 기록", "로그로 확인"을 판정 수단으로
쓴다([`roadmap.md`](roadmap.md)). 무엇을 어디에 어떤 모양으로 내는지가 없으면 그 검증을
통과했는지 판정할 수 없다. **그 항목들이 말하는 "기록"은 이 로그다.** 로컬 기록 파일은 Phase
4의 산출물이라 Phase 2~3에는 아직 없다. 최소 계약은 넷이다.

| 항목 | 계약 |
|------|------|
| 출력처 | 표준 오류(stderr). 버퍼링에 갇히지 않아 크래시 직전 줄이 남고, 표준 출력과 따로 리다이렉트할 수 있다 |
| 수준 | `INFO`, `WARN`, `ERROR` 셋. 더 나누지 않는다 |
| 단위 | 한 줄에 이벤트 하나. **값에 공백과 제어문자를 싣지 않는다.** 둘 다 밑줄 하나로 바꾼다. 이스케이프 표기도 따옴표도 쓰지 않는다 |
| 실패 표기 | 8장의 실패 코드 문자열을 **그대로** 쓴다. 번역하거나 다른 말로 바꾸지 않는다 |

수준을 셋으로 묶는 이유는 판정에 쓰이는 축이 "정상 진행 / 계속 가능한 이상 / 그 시도의 실패" 셋뿐이기 때문이다. 더 나누면 구현마다 경계가 달라지고, 검증 항목은 여전히 문자열 하나를 찾을 뿐이다.

> **왜 밑줄인가.** 공백으로 바꾸면 `이름=값`의 경계가 사라진다. 사용자가 준 문자열을 그대로
> 싣는 필드가 있어 그 일이 실제로 일어난다. 따옴표로 감싸면 경계는 서지만 따옴표가 든 값을
> 위해 이스케이프가 필요해지고, 그러면 읽는 쪽이 그것을 해석해야 한다.

**치환은 값에만 적용하고 되돌릴 수 없다.** 원문이 필요한 판정을 로그로 하지 않는다. 로그는
사람이 읽는 진단이고, 판정의 입력은 로컬 기록 파일이다.

**판정은 바이트 단위다.** 바꾸는 것은 ASCII 공백(`0x20`)과 제어 바이트(`0x00`~`0x1F`,
`0x7F`) 뿐이다. `0x80` 이상은 그대로 싣는다. 그 바이트는 줄을 끊지 않고 `이름=값` 의 경계도
흐리지 않는다.

> **한계.** 유니코드 수준의 줄 구분자(U+2028 같은 것)는 바뀌지 않는다. 로그를 유니코드
> 줄바꿈으로 나누는 도구를 판정에 쓰지 않는다. 판정은 `0x0A` 로 나눈다.

**검증이 찾는 이벤트에는 고정 키를 준다.** 위 계약만으로는 "로그로 확인"하는 검증 항목이 무엇을 찾아야 하는지 정해지지 않는다. 줄은 `<수준> <이벤트키> <이름>=<값> ...` 모양이고, 검증이 쓰는 이벤트키와 필드는 아래로 고정한다. 값의 형식은 정하지 않는다. 검증은 키와 필드 이름으로 찾는다.

| 이벤트키 | 필수 필드 | 어느 검증이 쓰는가 |
|----------|-----------|--------------------|
| `socket.bind` | `local` (bind 한 실제 엔드포인트), `rcvbuf_requested`, `rcvbuf_applied` | Phase 1 bind 계약, `SO_RCVBUF` 적용값, [`spec.md`](spec.md) M-1 기동 판정 |
| `stun.result` | `server`, `mapped` (관측된 공인 엔드포인트) | Phase 2, M-2 |
| `session.state` | `from`, `to` ([`protocol.md`](protocol.md) 9.1 상태의 이름 문자열 그대로) | Phase 4 전이 확인 |
| `session.failed` | `code` (8장 문자열) | Phase 2~5 실패 코드 확인 |
| `counter` | `name`, `value` | 폐기 카운터를 보는 모든 항목 |
| `socket.error` | `op` (호출 이름), `code` (Winsock 오류 코드) | Phase 1 소켓 오류에도 프로세스가 죽지 않는지 |
| `control.result` | `op`, `ok`, `error` (실패 코드 문자열, 성공이면 `-`) | Phase 3 연산 검증, `CONTROL_PLANE_EXCHANGE_FAILED` 원인 확인. **`room_id` 와 `peer_token` 을 싣지 않는다** (3.5) |
| `control.peers` | `peer_id`, `virtual_ip`, `candidates` (상대 후보 `ip:port` 를 쉼표로 이어 붙인 것) | `spec.md` M-3, Phase 3 `get_peers` 검증 |
| `timer.tick` | `name`, `elapsed_ms` (직전 만료로부터 실제로 흐른 밀리초) | Phase 1 drain 예산, Phase 3 제어 평면 사망 중 `[loop]` 생존 |
| `rx.raw` | `from`, `len`, `sha256` | Phase 1 원시 송수신 대조 (3.5) |

여기 없는 이벤트는 자유롭게 늘려도 된다. **이 열 개의 키와 필드 이름만 바꾸지 않는다.** 바꾸면 검증 항목이 같이 낡는다.

**`timer.tick` 은 시험 빌드에만 있는 타이머가 낸다.** 제품 빌드에는 없다. 시험 빌드는 주기
200ms 짜리 타이머 하나(`name=probe200`)를 `[loop]` 의 타이머 집합에 넣고, 만료할 때마다 이
줄을 낸다. **`elapsed_ms` 는 로그 줄에 시각이 없어도 간격을 판정할 수 있게 하려는 것이다.**
검증이 보는 값이 그것 하나이므로 줄 자체의 시각 형식을 정할 필요가 없다. 켜는 방법은 에코
응답기와 같은 CMake 옵션이고(`roadmap.md` Phase 4), 켜진 빌드는 기동 시
`WARN` 한 줄을 남긴다.

**검증이 값을 비교하는 필드는 형식도 고정한다.** 형식을 정하지 않으면 `256KB` 와 `262144` 와 `0x40000` 중 무엇을 비교할지 검증자가 정하게 된다.

| 필드 | 형식 |
|------|------|
| `rcvbuf_requested`, `rcvbuf_applied` | 10진 바이트 수. 단위 접미사를 붙이지 않는다 |
| `local`, `mapped` | `IPv4:port` 10진 표기 |
| `virtual_ip` | 점 십진 IPv4 |
| `candidates` | `IPv4:port` 를 쉼표로 이어 붙인 것. 공백 없음 |
| `code` | 8장의 실패 코드 문자열, 또는 `socket.error` 에서는 10진 오류 코드 |
| `error` (`control.result`) | [`control_plane.md`](control_plane.md) 4.1 공통 봉투의 오류 코드 문자열, 전송 오류면 `transport`, 성공이면 `-` |
| `value` (`counter`) | 10진 정수 |

나머지 필드의 값 형식은 정하지 않는다. 사람이 읽는 진단이다.

**`counter` 이벤트를 언제 내는지도 정한다.** 시점이 없으면 카운터를 보는 검증 항목이 무엇을
기다려야 하는지 알 수 없다. 세 시점이다.

- 세션이 종료 상태(`FAILED`, `CLOSED`)에 도달할 때 그 세션의 카운터 전량
- 프로세스 종료 절차(3.2.7)에서 전량
- `[console]` 명령으로 요청받았을 때 즉시 전량

**증가할 때마다 내지 않는다.** 폐기 카운터는 공격 트래픽에서 초당 수천 번 오를 수 있고,
그때마다 로그를 쓰면 로그가 데이터 평면의 부하가 된다. 값이 0인 카운터도 함께 낸다. 없는
것과 0인 것을 구분해야 검증이 "오르지 않았다"를 판정할 수 있다.

**로그는 위의 로컬 기록 파일과 다른 물건이다.** 로그는 사람이 읽는 진단이라 줄이 늘거나
문구가 바뀌어도 되고, 기록 파일은 `spec.md` M-6 판정의 입력이라 위의 계약을 지킨다.

**M-6 판정을 로그에서 읽지 않고, 진단을 기록 파일에 늘리지 않는다.** 둘을 섞으면 진단 문구를
고칠 때마다 M-6 판정이 같이 흔들린다.

**Phase 1~3 검증이 위의 고정 이벤트 키를 읽는 것은 다른 이야기다.** 그 Phase 에는 기록 파일이
아직 없고, 그래서 키와 필드 이름을 고정해 둔 것이다.

---

## 10. 레포 구조

### 목표 구조

```text
Sangtachi/
+-- client/
|   +-- include/
|   +-- src/
|   |   +-- main.cpp
|   |   +-- args.cpp     기동 인자 파싱 (3.5)
|   |   +-- network/     wsa, udp_socket, endpoint, stun_client
|   |   +-- peer/        peer, hole_punch, session
|   |   +-- tunnel/      packet, tunnel, router
|   |   +-- adapter/     wintun_adapter
|   |   +-- telemetry/   telemetry
|   |   +-- control/     control_client
|   +-- CMakeLists.txt
+-- control-server/      모듈 책임은 control_plane.md 7.1
|   +-- server.py
|   +-- ops.py
|   +-- ids.py
|   +-- candidates.py
|   +-- clock.py
|   +-- store.py
|   +-- tests/
+-- telemetry-server/
|   +-- server.py
|   +-- ingest.py
+-- tests/
+-- scripts/
+-- docs/
|   +-- kor/                 한국어 문서 (이 트리)
|   |   +-- architecture.md      이 문서
|   |   +-- spec.md              요구사항과 성공 기준
|   |   +-- roadmap.md           단계별 개발 계획
|   |   +-- plan.md              다음 세션 인수인계
|   |   +-- protocol.md          터널 프로토콜 확정본
|   |   +-- control_plane.md     제어 평면 확정본
|   |   +-- experiments.md       실험 설계와 측정 결과 (Phase 9에서 작성)
|   |   +-- audit-history/       전면 설계 점검 기록 보관소
|   |   +-- decisions/           설계 결정 기록. ADR 과 first_design.md
|   |   +-- commit_history/      작업 단위 변경 기록
|   +-- eng/                 영어 문서, 동일 구조
+-- README.md
```

두 언어 트리는 같은 파일을 갖는다. 문서를 고칠 때는 같은 커밋에서 양쪽을 함께 수정한다. **예외는 `plan.md` 하나다.** 다음 세션 인수인계 문서라 번역본을 두면 낡은 상태가 두 곳에 남는다. 문서 게이트가 그 예외를 케이스로 고정한다.

### 현재 상태와의 차이

현재 소스 트리는 루트의 `src/main.cpp`와 `CMakeLists.txt`뿐이다 (`docs/`는 이미 채워져 있다). `client/` 하위로 옮기고 `control-server/` 와 `telemetry-server/` 를 만드는 재배치가 남아 있다.

---

## 11. 외부 의존성

| 의존성 | 제공 범위 | 승인 상태 |
|--------|-----------|-----------|
| Wintun | Windows 가상 네트워크 인터페이스 접근만. 어댑터 생성, 패킷 read/inject | **승인됨** |
| Windows IP Helper / NetIO API | 가상 어댑터의 IP 주소와 라우트 설정. **Wintun은 이 기능을 제공하지 않는다** | OS 기본 제공 |
| Winsock2 | Windows 기본 소켓 API | OS 기본 제공 |
| 공개 STUN 서버 | Binding Response 응답. 서버는 구현하지 않고 이용만 한다 | 외부 공개 서비스 |
| Python 표준 라이브러리 | `asyncio`, `json` | 표준 라이브러리 |
| AWS SDK for Python (`boto3`) | 제어 서버와 텔레메트리 서비스의 DynamoDB 접근 | **승인됨** |
| Catch2 v3 | 시험 케이스 등록, 실행, 실패 보고. 시험 실행 파일에만 링크한다 | **승인됨.** 승인 주체는 [`spec.md`](spec.md) NFR-5 |
| Amazon DynamoDB | 방·피어 상태와 지표의 영속 저장 | 외부 관리형 서비스 |

Wintun이 제공하지 **않는** 것을 명확히 한다. 피어 발견, STUN, NAT 통과, 홀펀칭, 터널 프로토콜, 라우팅 결정, 세션 관리, 모니터링, 진단은 모두 이 프로젝트가 직접 구현한다. Wintun은 커널 모드 가상 NIC 드라이버에 대한 접근 수단일 뿐이다.

---

## 12. 초기 범위 밖

다음은 의도적으로 초기 아키텍처에 넣지 않는다. 필요해지면 [`decisions/`](decisions/)에 결정 기록을 남기고 도입한다.

- **암호화 및 피어 인증**: 터널 페이로드는 평문이다. 스트레치 목표.
- **릴레이 폴백 (TURN 유사)**: 직접 연결 실패 시 대안 경로 없음. 스트레치 목표.
- **완전한 ICE**: 초기에는 최소 연결 수립 절차만. Phase 9에서 ICE 개념과 비교 분석만 수행.
- **3자 이상 메시**: 초기는 2 피어. 라우팅 테이블 구조는 확장 가능하게 두되 구현하지 않는다.
- **GUI**: 콘솔 전용.
- **macOS / 모바일**: 지원하지 않는다. Linux는 시간이 남으면 상호 운용성만 검토.
