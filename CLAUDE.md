# CLAUDE.md

CSP 400 캡스톤. Windows 우선 direct-first P2P 가상 네트워크.
클라이언트는 C++20 / Winsock2 / Wintun, 제어 평면은 Python / AWS EC2.

셸은 Linux지만 **클라이언트의 개발과 실행 대상은 Windows 10/11 x64다.** Linux에서 빌드되는지
걱정하지 않는다. 제어 서버는 Linux/AWS EC2가 대상이다.

## 문서 위치

전역 규약이 말하는 `docs/spec.md`, `docs/plan.md`, `docs/decisions/`, `docs/commit_history/`는
이 레포에 그 경로로 존재하지 않는다. 언어별로 한 단계 아래다.

```
docs/kor/    한국어. 원본
docs/eng/    영어. 미러
```

세션 시작 시 다음 순서로 읽는다.

1. `docs/kor/spec.md`
2. `docs/kor/commit_history/` 최신 1~2건
3. `docs/kor/decisions/`
4. `docs/kor/plan.md`
5. `docs/kor/roadmap.md` 중 착수할 Phase

**살아 있는 상태는 `plan.md`에서 읽는다.** 감사 기록은 그 시점의 스냅숏이라 현재 상태의
출처가 아니다.

남은 작업은 세 군데로 나뉜다. 하나만 보고 전체를 안다고 판단하지 않는다.

| 무엇 | 어디 |
|------|------|
| 설계 감사 후속 | `plan.md` "설계 감사 후속" 절. **현재 6건 전부 완료다** |
| Phase 1~9 구현 작업 | `roadmap.md` |
| 문서 부채 | `plan.md` "문서 부채" 절 |

## 문서 역할

| 문서 | 역할 |
|------|------|
| `spec.md` | 요구사항 FR/NFR/C, 성공 기준 M/T/A |
| `roadmap.md` | Phase 1~9. 목표, 작업, 산출물, 검증 |
| `architecture.md` | 시스템 구성, 모듈 분해, 동시성 모델(3.2), 데이터 평면 경로 |
| `protocol.md` | **터널/STUN 와이어 프로토콜의 단일 출처.** 상수, 오프셋, 타이머 값, 전이표, 검증 파이프라인. 14장은 제어 평면이 지켜야 할 계약만 적고 REST/JSON 인코딩은 정하지 않는다 |
| `windows-prereq.md` | **실행 전제의 단일 출처.** 관리자 권한, 방화벽, 어댑터 정리, 서브넷 충돌, Wintun 패키징, EC2. 절마다 확인 방법과 통과 조건을 적는다. 명령이 없는 절도 있다 |
| `plan.md` | **다음 세션 인수인계.** 남은 일, 대기 중인 것, 문서 부채. 끝난 일의 경과는 담지 않는다. **한국어만 둔다** |
| `first_design.md` | 최초 기획서. **참고 자료이며 확정 사양이 아니다.** 충돌 시 다른 문서가 우선 |

폴더는 셋이고 전부 **기록**이다. 살아 있는 상태를 여기서 읽지 않는다.

| 폴더 | 역할 | 고치는가 |
|------|------|----------|
| `audit-history/` | 전면 설계 점검 기록. blocker/warn 목록과 그때의 해소 경과 | **아니다.** 장 번호와 수치를 당시 표기 그대로 둔다 |
| `commit_history/` | 커밋 단위 기록. 변경, 결정, 검증, 크로스 모델 리뷰 결과 | **아니다.** 그 시점의 사실이다 |
| `decisions/` | ADR. 무엇을 정했고 **어떤 대안을 왜 버렸는지**와 그 대가 | 결정을 뒤집을 때만. 그때도 새 ADR을 쓰고 옛 것은 남긴다 |

**기록 폴더는 서로 링크해도 된다.** 그러라고 있는 문서다. 이 파일도 `commit_history/`와
`decisions/`를 근거로 인용한다. 규칙의 근거가 어느 커밋에서 나왔는지는 그 기록에만 있기
때문이다.

**막는 것은 둘이다.** (1) 살아 있는 문서(`spec.md`, `roadmap.md`, `architecture.md`,
`protocol.md`, `plan.md`, `windows-prereq.md`)가 기록을 근거로 링크하는 것. 그러면 확정
문서가 아닌 것이 출처가 된다. (2) 이 파일이 `audit-history/`를 가리키는 것. 규칙의 원문이
여기로 왔으므로 가리킬 이유가 없다.

**와이어 포맷, 상태 전이, 프로토콜 타이머 값**이 충돌하면 `protocol.md`가 맞다. 요구사항은
`spec.md`, 스레드와 루프 구조는 `architecture.md` 3.2가 각각 출처이며 `protocol.md`가 그것을
덮지 않는다. 구현 중 문서에 없는 값을 만나면 코드에서 임의로 정하지 말고 해당 문서를 먼저 고친다.

## 규칙

**한국어가 원본이고 영어가 미러다.** 한쪽만 고치지 않는다. **예외는 `plan.md` 하나다.** 인수인계 문서라 번역해 둘 이유가 없고, 번역이 늦으면 낡은 상태를 두 곳에 두게 된다. 예외 목록은 `docgate.py` 의 `KOR_ONLY` 에 있고 케이스로 고정했다. **없어도 되는 것이지 `docs/eng` 에 있어도 되는 것이 아니다.** **리뷰에 보내기 전에 문서 게이트를
돌린다.** 미러 짝, 헤딩 구조, 표·코드 블록·링크 수, 상대 링크를 판정한다.

```bash
python tools/docgate/docgate.py            # 마지막 줄이 VERDICT: pass 여야 한다
python tools/docgate/docgate.py --claims   # 강한 주장 문구 목록. 종료 코드는 바뀌지 않는다
```

검사 내용과 예외는 `tools/docgate/docgate.py` 첫머리에 적혀 있다. 끊긴 채로 두어도 되는 링크는
`experiments.md` 하나뿐이다 (Phase 9 산출물). 미러 짝이 없어도 되는 문서는 `plan.md` 하나뿐이다.

**살아 있는 문서에는 날짜와 감사 식별자를 적지 않는다.** `spec.md`, `roadmap.md`,
`architecture.md`, `protocol.md`, `windows-prereq.md` 는 **현재 사양**만 담는다. 언제 무엇이
해소됐는지는 `commit_history/` 와 `audit-history/` 가 갖는다. 근거가 필요하면 날짜로
대신하지 말고 **무엇을 어떻게 측정했는지를 그 자리에 적는다.** 날짜는 근거가 아니다.
`plan.md` 는 예외다. 작업 추적이 그 문서의 일이다.

**작업 타이밍은 `roadmap.md` 가 갖는다.** "Phase 4 착수 전에 정한다" 같은 문장을 `spec.md`,
`architecture.md`, `protocol.md` 에 적지 않는다. 그 셋은 요구사항, 설계, 와이어 프로토콜의
출처이지 일정표가 아니다. **"Phase 1~5 에는 어댑터가 없다" 같은 범위 서술은 타이밍이
아니므로** 남긴다. 그 단계에서 설계가 어떤 모습인지를 말하는 것이다.

**의미 있는 커밋마다 `docs/{kor,eng}/commit_history/YYYY-MM-DD-주제.md`를 남긴다.**
변경, 결정, 검증, 크로스 모델 리뷰 결과(지적별로 반영 또는 기각 사유)를 적는다.

**설계 결정은 `docs/{kor,eng}/decisions/`에 ADR로 남긴다.** 현재 2건 있다.

**이 파일에 규칙을 새로 넣을 때는 저장소 소유자에게 먼저 허락을 받는다.** 하위 에이전트의
동의나 크로스 모델 리뷰의 `LGTM` 은 허락이 아니다. 세션에서 배운 것을 그 자리에서
적지 않는다. 무엇을, 어느 절에, 무슨 근거로 넣을지 묶어서 제안하고 **승인된 것만 적는다.**
이 파일은 다음 세션의 행동을 정한다. 한 세션의 인상이 검토 없이 영구 규칙이 되면, 틀린
규칙을 다음 세션이 그대로 따른다. 승인받지 못한 관찰은 `commit_history/` 에만 남긴다.
**낡은 사실을 고치는 것과 현재 상태를 갱신하는 것은 허락 대상이 아니다.** 규칙을 새로
만들거나 뜻을 바꾸는 것만이다.

**커밋 전 크로스 모델 리뷰를 돌린다.** `cross-review` 스킬. 게이트 훅이 스테이징된 diff의
sha256으로 확인한다. 리뷰 없이 마커를 찍지 않는다.

## 문서 작업 시

**재발 방지 규칙 7개다. 이 파일이 원문이다.** 2026-09-14 전면 점검과 2026-09-20 blocker 20
작업에서 얻었다. 그 경위는 감사 기록에 남아 있으나, 지키라고 있는 규칙의 출처는 여기다.

1. **문서를 완성했다고 선언하기 전에 시나리오 하나를 끝에서 끝까지 추적한다.** 패킷 한 개,
   실패 한 건, 재시작 한 번. 컴포넌트 목록과 검증 항목만으로는 구멍이 안 보인다.
2. **diff 리뷰는 국소 결함만 잡는다.** **Phase 를 끝낼 때마다** 전문 대상 감사를 따로
   돌린다. 원문은 "마일스톤마다"였다. 마일스톤 구분을 없앴으므로 주기가 세 번에서 Phase
   수만큼으로 **잦아졌다.** 대신 범위를 좁힌다. 그 Phase 가 건드린 문서와 코드만 본다.
   전 범위 감사는 `roadmap.md` 의 우선순위 등급이 바뀔 때만 돌린다.
3. **검증 기준은 정상 구현이 통과하는지부터 확인한다.** 통과 못 하는 기준을 넣으면 나중에
   기준을 낮추게 되고, 그때는 진짜 결함도 같이 통과한다.
4. **지적을 반영할 때 그 수정이 만드는 새 계약을 함께 정의한다.** "같은 소켓을 쓴다"는
   제약을 넣었으면 그 소켓의 소유권과 역다중화도 같이 정해야 했다.
5. **같은 주장을 두 곳 이상에 복사하지 않는다.** 주장은 한 곳에 쓰고 나머지는 링크한다.
   복사하면 강도를 낮출 때마다 전부 따라가야 하는데 반드시 한두 곳을 흘린다. 이미 흩어져
   있다면 고치기 전에 `grep` 으로 전수 확인하고 한 번에 맞춘다. **예외는 셋이다.**
   (1) `docs/kor` 와 `docs/eng` 의 미러 쌍(한국어가 원본). (2) **요약 색인.** 규칙 이름
   한 줄까지만 적고 본문을 옮기지 않으며 원문 위치를 밝힌다. (3) **역사 기록** —
   `audit-history/`, `commit_history/`, `decisions/`, 실측 기록. 이들은 특정 시점의 사실을
   적는 문서이므로 그때의 근거와 수치를 그대로 담는다. **원문이 바뀌어도 고치지 않는다.**
   링크만 남기면 나중에 읽을 때 그 시점의 판단이 아니라 현재 문서를 보게 되어 기록의 목적이
   사라진다. **`decisions/` 는 단서가 하나 더 붙는다.** 같은 결정이 살아 있는 문서에도 있으면
   출처는 살아 있는 문서이고 ADR 은 그때의 판단 기록이다. 결정을 뒤집을 때는 **새 ADR 을 쓰고
   옛 것을 고치지 않는다.**
6. **문서에 실행 가능한 절차를 넣으려면 코드와 같은 수준으로 검토한다.** 입력 검증, 중단 시
   정리, 권한 범위, 결과 해석 범위까지 본다. 그럴 자신이 없으면 절차를 도구에 넣고 문서는
   가리키기만 한다. **아직 실행하지 않은 시험의 스크립트는 문서에 두지 않는다.**
7. **판정을 내는 함수는 검증을 먼저 쓴다.** 기본값은 "모르면 판정 불가"다. 확실할 때만
   단정한다. 표준 라이브러리 술어를 이름만 보고 믿지 않는다 (`is_private` 는 루프백과
   문서용 대역을 포함하고, `is_global` 은 멀티캐스트에 참이다). 허용 목록이 배제 목록보다
   안전하다.

**3번과 4번을 반복해서 어겼다.** 5~7번은 2026-09-20에 같은 실수를 또 하고 추가한 것이다.
그때 19라운드 93건 중 56건이 두 곳에 몰렸다. `natprobe.py` 의 판정 함수 31건(규칙 7)과
ADR 0002 의 미실행 PowerShell 절차 25건(규칙 6)이다.

리뷰 라운드는 깨끗한 결과가 나올 때까지 돌린다. 2026-09-20 blocker 20 작업은 **19라운드에
93건**이 걸렸다.

**리뷰에 보내기 전에 내 diff를 내가 먼저 자기모순 검사한다.** 주장 강도를 낮췄으면 `grep`으로
그 주장이 있는 모든 곳을 찾아 한 번에 맞춘다. 이걸 안 하면 라운드가 두 배로 늘어난다.
`docgate.py --claims` 가 그 목록을 낸다.

**`grep` 으로 훑을 때 확장자를 제한하지 않는다.** `plan.md` 를 번호 없는 문서로 다시 쓰고
죽은 참조를 `--include=*.md` 로만 찾았다. 나머지가 `.py` 와 `.gitignore` 에서 세 번에 걸쳐
따로 나왔다. 구조를 바꿨으면 `.git` 만 빼고 전부 본다.

**무엇을 지우기 전에 그것이 지고 있던 의무를 어디에 걸지 먼저 정한다.** 규칙 4의 적용이다.
`spec.md` 와 `architecture.md` 에서 타이밍 문장 다섯 개를 뺄 때 넷은 `roadmap.md` 에 이미
있었고 **하나는 어디에도 없었다.** 확인하지 않았으면 조용히 사라졌다.

**문서를 줄일 때는 남길 목록을 먼저 만든다.** 분량을 목표로 삼으면 먼저 지우고 리뷰가
되살리게 되어 라운드가 몇 배로 는다. 남길 기준은 **"이걸 모르면 틀린 결과를 맞다고 믿게 되는
문장"** 이다. 근거는 `docs/kor/commit_history/2026-09-21-tool-readmes.md` 다. nat-probe
README 를 515에서 90줄로 자르면서 안전 경고 일곱 종류가 빠졌고 여섯 라운드가 들었다.

**판정을 내는 절차는 케이스 표를 먼저 쓰고, 그 표를 돌릴 수 있는 자리에 둔다.** 위 규칙 7을
문서 안 절차에까지 넓힌 것이다. 케이스 표가 없으면 리뷰어가 반례를 한 라운드에 하나씩 던지게
되고, 라운드 수가 결함 수가 아니라 반례 수를 따라간다. 근거는
`docs/kor/commit_history/2026-09-21-docgate.md` 다.

**리뷰 지적을 기각하면 그 결정을 두 곳에 적는다. 문서 안과 다음 라운드 프롬프트다.** 커밋
기록에만 적으면 다음 리뷰어는 그것을 보지 않으므로 같은 지적이 라운드마다 다시 올라온다.
문서에 적으면 레포를 읽는 리뷰어가 알고, 프롬프트에 적으면 그 라운드가 그 자리를 건너뛴다.
근거는 `docs/kor/commit_history/2026-09-21-followup4-spec-logic.md` 의 2라운드다.

## 리뷰를 돌릴 때

**렌즈를 바꿔 여러 번 돌린다. 한 렌즈의 `LGTM` 은 깨끗하다는 뜻이 아니다.** 같은 diff 에
일반 검사를 돌려 `LGTM` 을 받고, "근거를 지워서 과장이 됐는가" 로 바꿔 돌리니 16건이 나왔다.
**지우는 diff 는 특히 그렇다.** 없어진 것은 diff 에 보이지만 없어져서 생긴 구멍은 안 보인다.

**리뷰어가 diff 밖을 놓칠 수 있다.** 앞 커밋에서 이미 고친 것을 "이 diff 에 없다" 로 지적한
일이 두 번 있었다. 레포를 읽으라고 프롬프트에 적어도 그렇다. **"없다" 류의 지적은 HEAD 를
직접 확인한 뒤에 받거나 기각한다.**

**틀린 지적도 빈자리를 가리킬 수 있다.** `run_punch` 에 없는 결함을 지목받았는데, 그 의심이
가능했던 이유는 **그 함수를 도는 시험이 하나도 없어서** 였다. 기각하면서 시험을 채웠다.
**기각은 지적을 버리는 것이 아니다.**

**리뷰어가 지어낸 경고는 넣지 않는다.** 그럴듯하고 기술적으로 말이 돼도 근거가 없으면
검증되지 않은 주장을 문서에 올리는 것이다. 없는 경고를 넣는 것은 있는 경고를 빼는 것과
같은 종류의 결함이다.

## 코드 작업 시

**케이스를 썼다고 검증된 것이 아니다. 결함을 일부러 넣어 그 케이스가 `FAIL` 하는지 본다.**
"밀린 주기를 따라잡지 않는다" 케이스를 만들고 그 방지 코드를 지웠는데 **그대로 통과했다.**
판정 축이 틀렸기 때문이다(횟수로 봤는데 간격을 봤어야 했다). 변이가 통과하면 그 케이스는
아직 아무것도 지키지 않는다.

**리팩터로 공유 지점을 만들면 그 자리에 시험이 있는지 먼저 본다.** 두 측정 루프의 뼈대를
한 함수로 뽑았는데 기존 시험 151건이 **두 함수를 하나도 덮지 않았다.** 시험 없는 단일 실패
지점을 만든 것이다.

**프로세스를 시작했다고 말하기 전에 핸들 상태를 확인한다.** 그리고 **한 셀에 편집, 상태
변경, 프로세스 시작을 같이 넣지 않는다.** 앞줄에서 예외가 나면 뒷줄이 조용히 사라진다.
2026-09-21 에 리뷰를 시작하는 줄이 실패한 셀 뒤쪽에 있어 실행되지 않았는데 "돌리는 중" 이라고
보고했다. 시작 여부를 묻는 질문이 없었으면 모른 채 지나갔다.

**측정하는 코드는 조용히 틀린다.** 크래시하지 않고 틀린 값을 낸다. 그래서 리팩터 리뷰의
렌즈는 "동작이 보존됐는가" 여야 하고, 마감 시각, 송신 일정, `select` 타임아웃 계산, 어느
소켓을 읽는가, 삼키는 오류, 중단 처리까지 하나씩 대조한다.

## 현재 상태

코드는 `CMakeLists.txt`와 hello 수준 `src/main.cpp`뿐이다. Phase 1은 CMake 구성만 되어 있고
Winsock2 래퍼부터는 미착수다.

**설계 감사 후속 6건이 전부 끝났다.** 설계 문서 쪽에서 구현을 막는 것은 없다. `plan.md` 참고.
4번에서 `DATA` 최소 왕복과 로컬 기록을 Phase 4로 당겨 **최소 성공 M-1 ~ M-6이 P0만으로
성립**하게 했고, **마일스톤 구분을 없앴다.** Phase를 주차에 못박지 않는다.

**로컬 기록 파일의 경로와 줄 형식은 아직 정하지 않았다.** 계약 4개만 `architecture.md` 9장에
있다. 정하기 전에는 M-6을 판정할 수 없다. 언제 정하는지는 `roadmap.md` Phase 4 의 착수 전
항목에 있다. 코드에서 임의로 정하지 않는다.

`docs/kor` 와 `docs/eng` 는 맞춰져 있고 `docgate.py` 가 통과한다. **작업할 때는 한국어를
먼저 고치고 푸시 지시가 있을 때 미러를 만든다.** 그 사이에는 게이트가 `mirror` 와 `parity` 로
막히는 것이 정상이다.

`decisions/` 에 ADR 2건이 있다. 도구는 셋이고 각각 README 가 있다.

| 도구 | 무엇 | 시험 |
|------|------|------|
| `tools/docgate/` | 문서 게이트 | 109건 |
| `tools/winprereq/` | `windows-prereq.md` 2절·7절 판정 스크립트 | `-SelfTest` 로 225건과 85건 |
| `tools/nat-probe/` | NAT 매핑과 홀펀칭 실측. 실측 22건을 마쳤다 | 163건 |

방화벽 시험 스크립트는 기계 검증까지 끝났다. 근거는 커밋 `c48d9dd` 와 그 기록
`docs/{kor,eng}/commit_history/2026-09-20-firewall-script-first-run.md` 다. **실측은 상대가
필요해 대기다.**
실측 원본과 기록은 공인 IP 때문에 `.gitignore` 로 로컬 보관한다.
