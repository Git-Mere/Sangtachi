# Split the Concurrency Model into `concurrency.md`

## Changes

- **New** `docs/{kor,eng}/concurrency.md`. The whole body of `architecture.md` 3.2 Concurrency
  Model moved here, as chapter 0 Where This Document Sits plus chapters 1-8
- `architecture.md` 3.2 keeps only the principle sentence, the four thread names and a link to the
  new document. **The section number stays, so 3.3 onward does not shift**
- Fixed references in the living documents. `protocol.md` 5, `roadmap.md` 15, `control_plane.md` 6,
  `windows-prereq.md` 1, `plan.md` 1, and `architecture.md` itself 9 (per language)
- 46 lines of code comments. 7 files under `client/`, 5 under `tests/`, and `scripts/cli-check.ps1`
- The document role table in `CLAUDE.md`, both living-document lists, the source sentence, and the
  current status

## Decisions

**Chapter numbers map one to one onto the old subsections.**

| Old number | New chapter | Title |
|------------|-------------|-------|
| 3.2.1 | chapter 1 | Threads |
| 3.2.2 | chapter 2 | Waiting |
| 3.2.3 | chapter 3 | One Loop Iteration |
| 3.2.4 | chapter 4 | Timers |
| 3.2.5 | chapter 5 | State Ownership |
| 3.2.6 | chapter 6 | Telemetry Isolation |
| 3.2.7 | chapter 7 | Shutdown |
| 3.2.8 | chapter 8 | The `[control]` Thread |

**This follows the `control_plane.md` split.** The original section shrinks to a summary and a
link, and its section number stays.

**Old numbers in the record folders were not touched.** `audit-history/`, `commit_history/` and
`decisions/` state the facts of their own moment.

**The `3.2 Address` section of `control_plane.md` was left alone.** It only shares the number; it
is a section of a different document.

## Verification

- `docgate.py` gives `VERDICT: pass` with `pairs=59`, `links=580`, `findings=0`. Mirror pairs,
  heading structure, table row counts, code block counts and link counts match on both sides
- Not one non-comment line changed in the code files, confirmed with `git diff -U0`
- The 3 remaining `architecture.md` 3.2 references are intended: the "is a summary" sentence in
  both `concurrency.md` files and the formatting example in `CLAUDE.md`
- 111 tests in `tools/docgate/`

## Approval

**The four rule sentences in `CLAUDE.md` were changed on the repository owner's instruction.** The
role table row, both living-document lists, the source sentence, and the document count in the
readability rules. The session reading order was not changed.

## Cross-Model Review

Three calls were run with Codex, two lenses each. The full diff is 225KB, so each call was cut
down to the files its lenses actually look at. Findings had to carry the lens name, so a missing
lens would be visible.

| Call | Scope | Lenses | Result |
|------|-------|--------|--------|
| 1 | Korean `concurrency.md` and `architecture.md` (74KB) | scenario trace, numbered references | `LGTM - no blockers` |
| 2 | The remaining Korean documents and code comments that were rewritten (54KB) | reference accuracy, `CLAUDE.md` consistency | 1 warn |
| 3 | Repair round. Only the increment of the fix and this record | soundness of the fix, honesty of the record | `LGTM - no blockers` |

**Call 3 could not open files.** The reviewer stated it judged only from what the diff
itself allows, so the number check is not backed by that call. The machine checks below are.

| Finding | Handling |
|---------|----------|
| warn. In `args.hpp`, the trailing `3.5` of `(concurrency.md 8장, 3.5)` reads as a section of `concurrency.md`, but startup input is `architecture.md` 3.5 | **Applied.** It now says `architecture.md 3.5` |

**The same mechanism was checked exhaustively.** Every place a reference was rewritten was scanned
by machine for a following number and which document it belongs to. Lines mentioning
`concurrency.md`, and the line after them, were collected wherever a number reference had no
document name in front of it. Only `args.hpp` was caught. The rest are chapters of
`concurrency.md` itself or false positives such as `HTTP/1.1`.

**Move fidelity was counted by machine,** not left to the reviewer. The Korean body goes from 3367
to 3377 tokens at a 0.993 match ratio, the English from 4664 to 4686 at 0.994. The 21 differences
on each side are all intended reference rewrites.
